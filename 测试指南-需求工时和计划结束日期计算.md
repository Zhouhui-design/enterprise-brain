# 测试指南 - 定时工额自动Lookup和计划结束日期计算

## 📋 功能概述

已完成以下两个核心功能的开发和修复：

### 1. ✅ 定时工额自动Lookup
- **规则**：lookup(产品物料库的"物料编号"=当前工序计划的"生产产品编号"，产品物料库的"定时工额")
- **触发条件**：当前工序计划"生产产品编号"不为空
- **实现位置**：
  - 前端：所有工序计划页面都有productCode监听器，自动调用物料库API
  - 后端：所有工序计划Service的create方法都有lookup逻辑

### 2. ✅ 计划结束日期自动计算
- **规则**：基于需补货数量、定时工额、计划开始日期和工序能力负荷表自动计算
- **触发条件**：需补货数量 > 0 && 定时工额 > 0
- **实现位置**：
  - 前端：自动查询计划结束日期
  - 后端：使用PlanEndDateCalculator统一计算

---

## 🧪 测试步骤

### 测试环境准备

1. **确保产品物料库有定时工额数据**
   ```bash
   # 访问产品物料库
   http://localhost:3003/material/list
   
   # 检查物料编号 6001A0306 的定时工额应该是 6.00
   ```

2. **启动后端服务**
   ```bash
   cd backend
   npm start
   ```

3. **启动前端服务**
   ```bash
   cd 07-frontend
   npm run dev
   ```

---

### 测试场景1：打包工序计划 - 新建记录

**目标**：验证定时工额自动lookup和计划结束日期自动计算

#### 步骤：

1. **打开打包工序计划页面**
   ```
   http://localhost:3003/production-planning/packing-process-plan
   ```

2. **点击"新建"按钮**

3. **填写必填字段**：
   - 打包工序计划编号：自动生成
   - 计划排程日期：选择今天或未来日期
   - 工序名称：输入"打包"
   - **生产产品编号：输入 `6001A0306`** ⬅️ **关键字段**
   - 需补货数量：输入 `100`

4. **观察自动填充**：
   - ✅ **定时工额应该自动填充为：6.00**（从产品物料库lookup）
   - ✅ **需求工时应该自动计算为：16.67**（100 / 6.00）
   - ✅ **计划结束日期应该自动计算**（基于需求工时和工序能力负荷表）

5. **检查浏览器控制台日志**：
   ```
   🔍 [定时工额Lookup] 查询产品物料库: 物料编号=6001A0306
   ✅ [定时工额Lookup] 找到定时工额: 6.00
   ```

6. **点击"确定"保存**

7. **验证数据库**：
   ```bash
   # 运行检查脚本
   cd /home/sardensy/enterprise-brain/enterpise-brain
   node check-standard-work-quota-issue.js
   
   # 应该看到：
   # ✅ 工序计划中的定时工额是: 6.00
   # ✅ 计划结束日期已计算
   ```

---

### 测试场景2：编辑已有记录 - 修改生产产品编号

**目标**：验证编辑时也能触发lookup

#### 步骤：

1. **在列表中点击"编辑"**

2. **修改生产产品编号**：
   - 改为其他有定时工额的物料编号，例如：`460183A`（定时工额 72.00）

3. **观察自动更新**：
   - ✅ **定时工额应该自动更新为：72.00**
   - ✅ **需求工时应该重新计算**
   - ✅ **计划结束日期应该重新计算**

4. **保存并验证**

---

### 测试场景3：验证不同工序

**目标**：验证所有工序计划都能正常工作

#### 测试的工序计划页面：

1. ✅ 打包工序计划 - `/production-planning/packing-process-plan`
2. ✅ 组装工序计划 - `/production-planning/assembly-process-plan`
3. ✅ 缝纫工序计划 - `/production-planning/sewing-process-plan`
4. ✅ 抛丸工序计划 - `/production-planning/shot-blasting-process-plan`
5. ✅ 人工焊接工序计划 - `/production-planning/manual-welding-process-plan`
6. ✅ 弯管工序计划 - `/production-planning/bending-process-plan`
7. ✅ 激光切管工序计划 - `/production-planning/laser-tube-cutting-process-plan`
8. ✅ 激光下料工序计划 - `/production-planning/laser-cutting-process-plan`
9. ✅ 人工下料工序计划 - `/production-planning/manual-cutting-process-plan`
10. ✅ 机器打磨工序计划 - `/production-planning/machine-grinding-process-plan`
11. ✅ 裁剪工序计划 - `/production-planning/cutting-process-plan`
12. ✅ 喷塑工序计划 - `/production-planning/spray-painting-process-plan`
13. ✅ 真工序计划 - `/production-planning/real-process-plan`

**每个页面都应该重复测试场景1的步骤**

---

## 🔍 问题诊断

### 如果定时工额仍然显示0.00：

1. **检查前端控制台**：
   ```
   - 是否有错误日志？
   - 是否有lookup的日志输出？
   ```

2. **检查数据库**：
   ```bash
   node check-standard-work-quota-issue.js
   ```

3. **检查物料库**：
   ```
   - 访问 http://localhost:3003/material/list
   - 确认物料编号存在且有定时工额
   ```

4. **检查后端日志**：
   ```bash
   cd backend
   # 查看后端日志，是否有lookup的日志
   ```

5. **清除浏览器缓存**：
   ```
   - 按 Ctrl+Shift+R 强制刷新
   - 或清除浏览器缓存
   ```

---

## ✅ 预期结果

### 成功标准：

1. ✅ **前端**：
   - 输入生产产品编号后，定时工额字段自动填充
   - 定时工额值与产品物料库一致
   - 需求工时自动计算正确
   - 计划结束日期自动计算正确

2. ✅ **后端**：
   - 创建工序计划时，定时工额自动从物料库lookup
   - 数据库中保存的定时工额值正确
   - 计划结束日期计算并保存

3. ✅ **控制台日志**：
   - 前端和后端都有清晰的lookup日志
   - 没有错误信息

---

## 📊 已完成的修复

### 后端修复（16个工序计划Service）：

1. ✅ `packingProcessPlanService.js`
2. ✅ `assemblyProcessPlanService.js`
3. ✅ `sewingProcessPlanService.js`
4. ✅ `shotBlastingProcessPlanService.js`
5. ✅ `manualWeldingProcessPlanService.js`
6. ✅ `bendingProcessPlanService.js`
7. ✅ `laserTubeCuttingProcessPlanService.js`
8. ✅ `laserCuttingProcessPlanService.js`
9. ✅ `drillingProcessPlanService.js`
10. ✅ `punchingProcessPlanService.js`
11. ✅ `manualCuttingProcessPlanService.js`
12. ✅ `machineGrindingProcessPlanService.js`
13. ✅ `cuttingProcessPlanService.js`
14. ✅ `sprayPaintingProcessPlanService.js`
15. ✅ `realProcessPlanService.js`
16. ✅ `materialPreparationPlanService.js`（备料计划）

### 前端修复（13个工序计划页面）：

1. ✅ `PackingProcessPlanList.vue`
2. ✅ `AssemblyProcessPlanList.vue`
3. ✅ `SewingProcessPlanList.vue`
4. ✅ `ShotBlastingProcessPlanList.vue`
5. ✅ `ManualWeldingProcessPlanList.vue`
6. ✅ `BendingProcessPlanList.vue`
7. ✅ `LaserTubeCuttingProcessPlanList.vue`
8. ✅ `LaserCuttingProcessPlanList.vue`
9. ✅ `ManualCuttingProcessPlanList.vue`
10. ✅ `MachineGrindingProcessPlanList.vue`
11. ✅ `CuttingProcessPlanList.vue`
12. ✅ `SprayPaintingProcessPlanList.vue`
13. ✅ `RealProcessPlanList.vue`

---

## 🎯 下一步

如果测试发现问题，请提供以下信息：

1. 具体操作步骤
2. 前端控制台日志
3. 后端日志（backend/logs/）
4. 数据库查询结果（运行check-standard-work-quota-issue.js）
5. 浏览器Network标签中的API请求和响应

---

**测试愉快！** 🎉
