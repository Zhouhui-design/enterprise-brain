# 📚 页面改造和菜单集成完整教程

## 目录
1. [页面改造模板](#一页面改造模板)
2. [菜单集成教程](#二菜单集成教程)
3. [完整示例](#三完整示例)
4. [常见问题](#四常见问题)

---

## 一、页面改造模板

### 1.1 改造前后对比

#### 改造前（使用原生 el-table）
```vue
<template>
  <div class="page-container">
    <!-- 工具栏 -->
    <div class="toolbar">
      <el-input v-model="searchKeyword" placeholder="搜索..." />
      <el-select v-model="filterStatus" placeholder="状态筛选" />
      <el-date-picker v-model="dateRange" type="daterange" />
    </div>

    <!-- 表格 -->
    <el-table :data="filteredData" v-loading="loading">
      <el-table-column type="selection" width="55" />
      <el-table-column prop="code" label="编号" />
      <el-table-column prop="name" label="名称" />
      <!-- 更多列... -->
    </el-table>

    <!-- 分页 -->
    <el-pagination
      v-model:current-page="currentPage"
      v-model:page-size="pageSize"
      :total="total"
    />
  </div>
</template>

<script setup>
import { ref, computed } from 'vue'

const searchKeyword = ref('')
const filterStatus = ref('')
const dateRange = ref(null)
const currentPage = ref(1)
const pageSize = ref(10)
const tableData = ref([])

// 复杂的筛选逻辑
const filteredData = computed(() => {
  let result = [...tableData.value]
  // 各种筛选条件...
  return result
})
</script>
```

#### 改造后（使用 FilterTable 组件）
```vue
<template>
  <div class="page-container">
    <div class="header">
      <h1>页面标题</h1>
      <div class="actions">
        <el-button type="primary" @click="handleAdd">新增</el-button>
        <el-button @click="handleExport">导出</el-button>
        <el-button @click="handlePrint">打印</el-button>
      </div>
    </div>

    <!-- 使用统一智能表格组件 -->
    <FilterTable
      ref="filterTableRef"
      :data="tableData"
      :columns="tableColumns"
      show-selection
      show-index
      @selection-change="handleSelectionChange"
      @filter-change="handleFilterChange"
    >
      <!-- 自定义列插槽 -->
      <template #status="{ row }">
        <el-tag :type="getStatusType(row.status)">
          {{ row.status }}
        </el-tag>
      </template>

      <!-- 操作列 -->
      <template #actions>
        <el-table-column label="操作" width="200" fixed="right">
          <template #default="scope">
            <el-button link type="primary" @click="handleEdit(scope.row)">编辑</el-button>
            <el-button link type="danger" @click="handleDelete(scope.row)">删除</el-button>
          </template>
        </el-table-column>
      </template>
    </FilterTable>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import FilterTable from '@/components/common/tables/FilterTable.vue'

const filterTableRef = ref(null)
const tableData = ref([])
const activeFilters = ref({})

// 表格列配置
const tableColumns = [
  { prop: 'code', label: '编号', width: 140, fixed: 'left', sortable: true },
  { prop: 'name', label: '名称', width: 180, sortable: true },
  { prop: 'status', label: '状态', width: 120 },
  // ... 更多列
]

// 筛选变化处理（FilterTable 自动处理）
const handleFilterChange = (filters) => {
  activeFilters.value = filters
  console.log('当前筛选条件:', filters)
}

// 选择变化处理
const handleSelectionChange = (selection) => {
  console.log('已选择:', selection)
}
</script>
```

---

### 1.2 详细改造步骤

#### 步骤 1：导入 FilterTable 组件

```javascript
// 在 <script setup> 中添加导入
import FilterTable from '@/components/common/tables/FilterTable.vue'
```

#### 步骤 2：定义表格列配置

```javascript
// 定义列配置数组
const tableColumns = [
  {
    prop: 'code',              // 字段名（必填）
    label: '设备编号',         // 列标题（必填）
    width: 140,                // 列宽度
    fixed: 'left',             // 固定列：'left' 或 'right'
    sortable: true,            // 是否可排序
    align: 'center'            // 对齐方式：'left', 'center', 'right'
  },
  {
    prop: 'name',
    label: '设备名称',
    width: 180,
    sortable: true
  },
  {
    prop: 'type',
    label: '设备类型',
    width: 120
  },
  {
    prop: 'status',
    label: '状态',
    width: 120
  },
  {
    prop: 'purchaseDate',
    label: '购买日期',
    width: 120
  }
  // ... 更多列配置
]
```

#### 步骤 3：删除原有的筛选、搜索、分页状态

```javascript
// ❌ 删除这些（FilterTable 会自动处理）
const searchKeyword = ref('')
const filterStatus = ref('')
const filterType = ref('')
const dateRange = ref(null)
const currentPage = ref(1)
const pageSize = ref(10)

// ❌ 删除筛选计算属性
const filteredData = computed(() => {
  // 复杂的筛选逻辑...
})

// ❌ 删除分页计算属性
const paginatedData = computed(() => {
  // 分页逻辑...
})

// ❌ 删除这些处理函数
const handleSearch = () => { ... }
const handleFilter = () => { ... }
const handleSizeChange = () => { ... }
const handleCurrentChange = () => { ... }
```

#### 步骤 4：添加 FilterTable 相关状态

```javascript
// ✅ 添加这些
const filterTableRef = ref(null)
const activeFilters = ref({})

// ✅ 添加筛选变化处理
const handleFilterChange = (filters) => {
  activeFilters.value = filters
  console.log('当前筛选条件:', filters)
}
```

#### 步骤 5：替换模板中的表格组件

```vue
<!-- ❌ 删除原有的工具栏 -->
<div class="toolbar">
  <el-input v-model="searchKeyword" />
  <el-select v-model="filterStatus" />
  <el-date-picker v-model="dateRange" />
</div>

<!-- ❌ 删除原有的 el-table -->
<el-table :data="filteredData" v-loading="loading">
  <el-table-column type="selection" />
  <el-table-column prop="code" label="编号" />
  <!-- ... -->
</el-table>

<!-- ❌ 删除原有的分页 -->
<el-pagination
  v-model:current-page="currentPage"
  v-model:page-size="pageSize"
/>

<!-- ✅ 替换为 FilterTable -->
<FilterTable
  ref="filterTableRef"
  :data="tableData"
  :columns="tableColumns"
  show-selection
  show-index
  @selection-change="handleSelectionChange"
  @filter-change="handleFilterChange"
>
  <!-- 自定义列插槽 -->
  <template #status="{ row }">
    <el-tag :type="getStatusType(row.status)">
      {{ getStatusText(row.status) }}
    </el-tag>
  </template>

  <!-- 操作列 -->
  <template #actions>
    <el-table-column label="操作" width="200" fixed="right">
      <template #default="scope">
        <el-button link type="primary" @click="handleEdit(scope.row)">编辑</el-button>
        <el-button link type="danger" @click="handleDelete(scope.row)">删除</el-button>
      </template>
    </el-table-column>
  </template>
</FilterTable>
```

#### 步骤 6：更新导出功能（获取筛选后的数据）

```javascript
// 导出功能
const handleExport = () => {
  // ✅ 从 FilterTable 获取筛选后的数据
  const dataToExport = filterTableRef.value?.filteredData || tableData.value
  
  if (dataToExport.length === 0) {
    ElMessage.warning('没有数据可导出')
    return
  }
  
  ElMessage.success(`成功导出 ${dataToExport.length} 条数据`)
}

// 打印功能
const handlePrint = () => {
  window.print()
}
```

---

### 1.3 自定义列插槽示例

```vue
<!-- 状态列 - 显示标签 -->
<template #status="{ row }">
  <el-tag 
    :type="row.status === 'active' ? 'success' : 'danger'"
    effect="light"
  >
    {{ row.status === 'active' ? '启用' : '禁用' }}
  </el-tag>
</template>

<!-- 利用率列 - 显示进度条 -->
<template #utilization="{ row }">
  <div class="utilization-container">
    <el-progress 
      :percentage="row.utilization" 
      :show-text="false" 
      :color="getUtilizationColor(row.utilization)"
    />
    <span style="margin-left: 5px">{{ row.utilization }}%</span>
  </div>
</template>

<!-- 图片列 - 显示图片 -->
<template #image="{ row }">
  <el-image 
    :src="row.imageUrl" 
    :preview-src-list="[row.imageUrl]"
    style="width: 50px; height: 50px"
  />
</template>

<!-- 日期列 - 格式化日期 -->
<template #createTime="{ row }">
  {{ formatDate(row.createTime) }}
</template>

<!-- 金额列 - 格式化金额 -->
<template #amount="{ row }">
  ¥{{ formatMoney(row.amount) }}
</template>
```

---

## 二、菜单集成教程

### 2.1 菜单结构说明

菜单配置在 `07-frontend/src/layout/index.vue` 的 `loadMenus()` 方法中：

```javascript
loadMenus() {
  this.menus = [
    {
      path: '/一级菜单路径',
      name: 'MenuName',
      meta: { title: '一级菜单标题' },
      icon: 'el-icon-xxx',
      children: [
        {
          path: '/二级菜单路径',
          name: 'SubMenuName',
          meta: { title: '二级菜单标题' },
          icon: 'el-icon-xxx',
          children: [
            {
              path: '/三级菜单路径',
              name: 'ThirdMenuName',
              meta: { title: '三级菜单标题' },
              icon: 'el-icon-xxx'
            }
          ]
        }
      ]
    }
  ]
}
```

### 2.2 菜单配置参数说明

| 参数 | 说明 | 类型 | 是否必填 | 示例 |
|------|------|------|----------|------|
| path | 路由路径 | String | 是 | '/equipment/list' |
| name | 路由名称（唯一） | String | 是 | 'EquipmentList' |
| meta.title | 菜单显示标题 | String | 是 | '设备列表' |
| icon | 图标名称 | String | 否 | 'el-icon-s-tools' |
| children | 子菜单数组 | Array | 否 | [] |
| meta.permissions | 权限控制 | Array | 否 | ['admin', 'user'] |

### 2.3 常用图标参考

```javascript
// Element Plus 内置图标
'el-icon-s-home'          // 首页
'el-icon-s-tools'         // 工具/设备
'el-icon-s-platform'      // 平台/模具
'el-icon-s-operation'     // 操作/生产
'el-icon-s-data'          // 数据/统计
'el-icon-s-order'         // 订单/计划
'el-icon-setting'         // 设置
'el-icon-document'        // 文档
'el-icon-edit'            // 编辑
'el-icon-box'             // 箱子/仓库
'el-icon-shopping-cart-2' // 购物车/采购
'el-icon-s-finance'       // 财务
'el-icon-s-check'         // 检查/质量
'el-icon-s-custom'        // 用户/人员
'el-icon-message'         // 消息
'el-icon-s-goods'         // 商品/物料
```

---

### 2.4 添加一级菜单（示例：生产资源管理）

在 `loadMenus()` 方法的 `this.menus` 数组中添加：

```javascript
{
  path: '/production-resources',
  name: 'ProductionResources',
  meta: { title: '生产资源管理' },
  icon: 'el-icon-s-tools',
  children: []
}
```

### 2.5 添加二级菜单（示例：设备管理）

在生产资源管理的 `children` 中添加：

```javascript
{
  path: '/production-resources',
  name: 'ProductionResources',
  meta: { title: '生产资源管理' },
  icon: 'el-icon-s-tools',
  children: [
    {
      path: '/equipment-management',
      name: 'EquipmentManagement',
      meta: { title: '设备管理' },
      icon: 'el-icon-s-operation',
      children: []
    }
  ]
}
```

### 2.6 添加三级菜单（示例：设备列表、设备维护等）

```javascript
{
  path: '/production-resources',
  name: 'ProductionResources',
  meta: { title: '生产资源管理' },
  icon: 'el-icon-s-tools',
  children: [
    {
      path: '/equipment-management',
      name: 'EquipmentManagement',
      meta: { title: '设备管理' },
      icon: 'el-icon-s-operation',
      children: [
        {
          path: '/production-resources/equipment/list',
          name: 'EquipmentList',
          meta: { title: '设备列表' },
          icon: 'el-icon-document'
        },
        {
          path: '/production-resources/equipment/maintenance',
          name: 'EquipmentMaintenance',
          meta: { title: '设备维护' },
          icon: 'el-icon-setting'
        },
        {
          path: '/production-resources/equipment/status',
          name: 'EquipmentStatus',
          meta: { title: '设备状态' },
          icon: 'el-icon-data-line'
        },
        {
          path: '/production-resources/equipment/repair',
          name: 'RepairManagement',
          meta: { title: '维修管理' },
          icon: 'el-icon-s-tools'
        },
        {
          path: '/production-resources/equipment/utilization',
          name: 'UtilizationReport',
          meta: { title: '利用率报告' },
          icon: 'el-icon-s-data'
        }
      ]
    }
  ]
}
```

---

## 三、完整示例

### 3.1 生产资源管理完整菜单配置

在 `07-frontend/src/layout/index.vue` 的 `loadMenus()` 方法中添加：

```javascript
{
  path: '/production-resources',
  name: 'ProductionResources',
  meta: { title: '生产资源管理' },
  icon: 'el-icon-s-tools',
  children: [
    // 设备管理
    {
      path: '/equipment-management',
      name: 'EquipmentManagement',
      meta: { title: '设备管理' },
      icon: 'el-icon-s-operation',
      children: [
        {
          path: '/production-resources/equipment/list',
          name: 'EquipmentList',
          meta: { title: '设备列表' },
          icon: 'el-icon-document'
        },
        {
          path: '/production-resources/equipment/maintenance',
          name: 'EquipmentMaintenance',
          meta: { title: '设备维护' },
          icon: 'el-icon-setting'
        },
        {
          path: '/production-resources/equipment/status',
          name: 'EquipmentStatus',
          meta: { title: '设备状态' },
          icon: 'el-icon-data-line'
        },
        {
          path: '/production-resources/equipment/repair',
          name: 'RepairManagement',
          meta: { title: '维修管理' },
          icon: 'el-icon-s-tools'
        },
        {
          path: '/production-resources/equipment/utilization',
          name: 'UtilizationReport',
          meta: { title: '利用率报告' },
          icon: 'el-icon-s-data'
        }
      ]
    },
    // 夹具管理
    {
      path: '/fixture-management',
      name: 'FixtureManagement',
      meta: { title: '夹具管理' },
      icon: 'el-icon-s-platform',
      children: [
        {
          path: '/production-resources/fixture/management',
          name: 'FixtureManagementPage',
          meta: { title: '夹具管理' }
        },
        {
          path: '/production-resources/fixture/inventory',
          name: 'FixtureInventory',
          meta: { title: '夹具库存' }
        },
        {
          path: '/production-resources/fixture/maintenance',
          name: 'FixtureMaintenance',
          meta: { title: '夹具维护' }
        },
        {
          path: '/production-resources/fixture/design',
          name: 'FixtureDesign',
          meta: { title: '夹具设计' }
        },
        {
          path: '/production-resources/fixture/setup',
          name: 'FixtureSetup',
          meta: { title: '夹具配置' }
        }
      ]
    },
    // 模具管理
    {
      path: '/mold-management',
      name: 'MoldManagement',
      meta: { title: '模具管理' },
      icon: 'el-icon-s-grid',
      children: [
        {
          path: '/production-resources/mold/management',
          name: 'MoldManagementPage',
          meta: { title: '模具管理' }
        },
        {
          path: '/production-resources/mold/maintenance',
          name: 'MoldMaintenance',
          meta: { title: '模具维护' }
        },
        {
          path: '/production-resources/mold/lifecycle',
          name: 'MoldLifecycle',
          meta: { title: '模具生命周期' }
        },
        {
          path: '/production-resources/mold/schedule',
          name: 'MoldSchedule',
          meta: { title: '模具排程' }
        },
        {
          path: '/production-resources/mold/storage',
          name: 'MoldStorage',
          meta: { title: '模具存储' }
        }
      ]
    },
    // 刀具管理
    {
      path: '/tooling-management',
      name: 'ToolingManagement',
      meta: { title: '刀具管理' },
      icon: 'el-icon-s-tools',
      children: [
        {
          path: '/production-resources/tooling/management',
          name: 'ToolingManagementPage',
          meta: { title: '刀具管理' }
        },
        {
          path: '/production-resources/tooling/inventory',
          name: 'ToolingInventory',
          meta: { title: '刀具库存' }
        },
        {
          path: '/production-resources/tooling/issue',
          name: 'ToolingIssue',
          meta: { title: '刀具发放' }
        },
        {
          path: '/production-resources/tooling/return',
          name: 'ToolingReturn',
          meta: { title: '刀具回收' }
        },
        {
          path: '/production-resources/tooling/maintenance',
          name: 'ToolingMaintenance',
          meta: { title: '刀具维护' }
        }
      ]
    }
  ]
}
```

### 3.2 插入位置参考

在 `07-frontend/src/layout/index.vue` 中找到这个位置：

```javascript
loadMenus() {
  this.menus = [
    // ... 其他菜单
    {
      path: '/production-management',
      name: 'ProductionManagement',
      meta: { title: '生产管理' },
      icon: 'el-icon-s-operation',
      children: [
        // 生产管理的子菜单...
      ]
    },
    
    // ✅ 在这里插入生产资源管理菜单
    {
      path: '/production-resources',
      name: 'ProductionResources',
      meta: { title: '生产资源管理' },
      icon: 'el-icon-s-tools',
      children: [
        // ... 上面的完整配置
      ]
    },
    
    // ... 后续菜单
    {
      path: '/planning-control',
      name: 'PlanningControl',
      meta: { title: '计划&物控管理' },
      // ...
    }
  ]
}
```

---

## 四、常见问题

### 4.1 菜单添加后不显示？

**检查点：**
1. 确认路由路径 `path` 是否正确
2. 确认路由名称 `name` 是否唯一
3. 确认 `meta.title` 是否填写
4. 确认是否保存并刷新页面

### 4.2 点击菜单后页面空白？

**检查点：**
1. 确认路由配置是否正确（在 `router/index.js` 中）
2. 确认页面文件是否存在
3. 确认组件导入路径是否正确

### 4.3 如何控制菜单权限？

```javascript
{
  path: '/admin/users',
  name: 'UserManagement',
  meta: { 
    title: '用户管理',
    permissions: ['admin']  // 只有 admin 角色可见
  }
}
```

### 4.4 FilterTable 组件的优势

✅ **自动处理筛选** - 无需手动编写筛选逻辑  
✅ **自动处理分页** - 内置分页功能  
✅ **自动处理排序** - 支持多列排序  
✅ **表头筛选** - 类似 Excel 的筛选体验  
✅ **列配置** - 可显示/隐藏列  
✅ **导出支持** - 轻松导出筛选后的数据  
✅ **打印支持** - 自动优化打印布局  

### 4.5 如何快速批量改造？

使用 VS Code 的查找替换功能：

1. **查找：** `<el-table :data="`
   **替换：** `<FilterTable ref="filterTableRef" :data="`

2. **查找：** `</el-table>`
   **替换：** `</FilterTable>`

3. **添加导入语句**
4. **添加列配置**
5. **删除筛选和分页逻辑**

---

## 五、快速开始检查清单

### ✅ 页面改造检查清单

- [ ] 导入 FilterTable 组件
- [ ] 定义 tableColumns 配置
- [ ] 删除原有的筛选、搜索、分页状态
- [ ] 添加 filterTableRef 和 activeFilters
- [ ] 替换 el-table 为 FilterTable
- [ ] 添加自定义列插槽（如需要）
- [ ] 更新导出功能（获取筛选后的数据）
- [ ] 测试功能是否正常

### ✅ 菜单集成检查清单

- [ ] 确定菜单层级（一级/二级/三级）
- [ ] 确定菜单标题和路径
- [ ] 选择合适的图标
- [ ] 在 loadMenus() 中添加配置
- [ ] 保存并刷新页面
- [ ] 点击菜单测试路由跳转
- [ ] 检查面包屑导航是否正确

---

## 六、获取帮助

如果遇到问题，请：
1. 参考已改造的示例页面：`/production-resources/equipment/EquipmentList.vue`
2. 查看 FilterTable 组件文档：`07-frontend/src/components/common/tables/FilterTable.README.md`
3. 检查浏览器控制台错误信息

---

**祝您改造顺利！🎉**
