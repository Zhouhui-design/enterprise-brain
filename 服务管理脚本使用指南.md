# 企业大脑系统 - 服务管理脚本使用指南

## 📋 脚本列表

### 1. **start-all-services.bat** - 启动所有服务
**用途**: 一键启动前后端服务

**执行步骤**:
1. 停止所有现有Node进程
2. 清理前端Vite缓存
3. 检查npm依赖
4. 启动后端服务（端口3005）
5. 等待后端就绪
6. 启动前端服务（端口3003）
7. 等待前端就绪
8. 显示服务状态
9. 自动打开浏览器

**使用方法**:
```batch
# 双击运行或命令行执行
start-all-services.bat
```

---

### 2. **stop-all-services.bat** - 停止所有服务
**用途**: 一键停止所有Node.js服务

**使用方法**:
```batch
stop-all-services.bat
```

---

### 3. **restart-services.bat** - 重启所有服务
**用途**: 停止并重新启动所有服务（修改代码后使用）

**使用方法**:
```batch
restart-services.bat
```

---

### 4. **check-services.bat** - 检查服务状态
**用途**: 查看当前服务运行状态和端口占用情况

**输出信息**:
- Node.js进程列表
- 端口占用情况（3003、3005）
- 服务健康检查

**使用方法**:
```batch
check-services.bat
```

---

### 5. **diagnose-startup-issues.bat** - 诊断启动问题
**用途**: 全面检查环境配置和依赖，诊断启动失败原因

**检查项目**:
- [ ] Node.js 和 npm 环境
- [ ] 项目目录结构
- [ ] package.json 文件
- [ ] node_modules 依赖
- [ ] 主要入口文件
- [ ] 端口占用情况
- [ ] 数据库配置
- [ ] npm缓存状态

**使用方法**:
```batch
diagnose-startup-issues.bat
```

---

## 🚀 快速使用指南

### 首次启动
```batch
# 1. 诊断环境
diagnose-startup-issues.bat

# 2. 启动服务
start-all-services.bat
```

### 日常开发
```batch
# 启动服务
start-all-services.bat

# 修改代码后重启
restart-services.bat

# 结束工作
stop-all-services.bat
```

### 遇到问题
```batch
# 1. 检查服务状态
check-services.bat

# 2. 如果有问题，运行诊断
diagnose-startup-issues.bat

# 3. 停止所有服务
stop-all-services.bat

# 4. 重新启动
start-all-services.bat
```

---

## 🛠️ 常见问题解决

### 问题1: 端口被占用
**症状**: 启动失败，提示端口3003或3005已被占用

**解决**:
```batch
stop-all-services.bat
start-all-services.bat
```

### 问题2: npm依赖缺失
**症状**: 启动时报错找不到模块

**解决**:
```batch
cd c:\Users\sardenesy\Projects\enterpise-brain\backend
npm install

cd c:\Users\sardenesy\Projects\enterpise-brain\07-frontend
npm install

start-all-services.bat
```

### 问题3: Vite缓存问题
**症状**: 前端页面无法加载或路由错误

**解决**:
```batch
# start-all-services.bat 会自动清理缓存
# 或手动清理：
rmdir /S /Q c:\Users\sardenesy\Projects\enterpise-brain\07-frontend\node_modules\.vite
```

### 问题4: timeout命令错误
**症状**: 执行脚本时提示"不支持输入重新定向"

**原因**: Windows的timeout命令在管道环境中有问题

**解决**: 所有脚本已改用PowerShell的Start-Sleep，不会再出现此问题

---

## 📊 服务端口说明

| 服务 | 端口 | 访问地址 | 说明 |
|------|------|----------|------|
| 前端 | 3003 | http://localhost:3003 | Vue3 + Vite |
| 后端 | 3005 | http://localhost:3005 | Express API |

---

## 💡 最佳实践

1. **每次开发前**: 运行 `start-all-services.bat`
2. **修改代码后**: 运行 `restart-services.bat`
3. **遇到问题时**: 先运行 `diagnose-startup-issues.bat`
4. **结束开发后**: 运行 `stop-all-services.bat`（可选，Windows会在关机时自动清理）

---

## 🔍 服务窗口说明

启动后会打开两个CMD窗口：

- **窗口标题: 后端-3005** - 后端服务日志
- **窗口标题: 前端-3003** - 前端服务日志

**⚠️ 不要关闭这两个窗口**，关闭会导致服务停止

---

## 📝 记忆规则

已创建以下记忆规则，AI助手会自动应用：

1. **Windows timeout命令问题**: 使用`powershell -Command "Start-Sleep N"`替代`timeout /t N`
2. **npm启动命令检查**: 执行npm命令前必须先检查package.json中的scripts配置

---

## 🆘 紧急恢复

如果所有脚本都失败，手动执行：

```batch
# 1. 杀死所有Node进程
taskkill /F /IM node.exe

# 2. 启动后端
cd c:\Users\sardenesy\Projects\enterpise-brain\backend
node server.js

# 3. 新开一个CMD窗口，启动前端
cd c:\Users\sardenesy\Projects\enterpise-brain\07-frontend
npm run dev
```

---

## 📞 技术支持

如果问题依然存在，请提供以下信息：

1. 运行 `diagnose-startup-issues.bat` 的完整输出
2. 后端窗口（后端-3005）的错误信息
3. 前端窗口（前端-3003）的错误信息
4. `check-services.bat` 的输出结果
