# 生产BOM数据持久化和材料单价更新说明

## 📅 更新时间
2025年12月2日

---

## ✅ 更新内容

### 一、生产BOM数据持久化保障 ✅

#### 当前保存机制（已实施）

**1. 数据库持久化**
```javascript
// 保存到后端数据库
const savedBom = await bomApiService.saveBom(newBom)

// 更新BOM
await bomApiService.saveBom(tableData.value[index])
```

**2. 保存时机**
- ✅ **保存按钮** - 保存到数据库，不关闭对话框
- ✅ **提交按钮** - 保存到数据库并关闭对话框
- ✅ **保存为草稿** - 保存草稿到数据库

**3. 数据不会丢失**
- ✅ 所有数据保存到SQLite数据库（`data/enterprise_brain.db`）
- ✅ 包含BOM主表和子件表
- ✅ 支持断电、重启、更新等情况
- ✅ 外键约束确保数据完整性

**4. 数据库表结构**
```sql
-- BOM主表
production_boms (
  id, bom_code, bom_name, product_code, product_name,
  version, status, designer, material_count, remark,
  auditor, effective_date, created_at, updated_at
)

-- BOM子件表
bom_components (
  id, bom_id, sequence, level, component_code, component_name,
  standard_quantity, output_process, component_source,
  process_wage, material_loss, material_price, material_cost,
  created_at, updated_at
)
```

---

### 二、材料单价改为基础单价 ✅

#### 修改内容

**1. 手动加载材料单价**
```javascript
// 修改前：从采购单价加载
if (material && material.purchasePrice) {
  row.materialPrice = material.purchasePrice
}

// 修改后：从基础单价加载
if (material && material.basePrice !== undefined && material.basePrice !== null) {
  row.materialPrice = material.basePrice
}
```

**提示消息：** `已加载 ${count} 条子件的材料单价（基础单价）`

---

**2. 自动填充（选择子件编码时）**
```javascript
// 修改前：从采购单价填充
if (material.purchasePrice) {
  row.materialPrice = material.purchasePrice
}

// 修改后：从基础单价填充
if (material.basePrice !== undefined && material.basePrice !== null) {
  row.materialPrice = material.basePrice
}
```

**提示消息：** `已自动填充子件信息（材料单价使用基础单价）`

---

**3. 自动填充（选择子件名称时）**
```javascript
// 修改前：从采购单价填充
if (material.purchasePrice) {
  row.materialPrice = material.purchasePrice
}

// 修改后：从基础单价填充
if (material.basePrice !== undefined && material.basePrice !== null) {
  row.materialPrice = material.basePrice
}
```

**提示消息：** `已自动填充子件信息（材料单价使用基础单价）`

---

**4. 额外增强：自动填充材料损耗**

在选择子件时，如果物料库有材料损耗数据，也会自动填充：
```javascript
// 材料损耗 = 物料库的材料损耗（如果有）
if (material.materialLoss !== undefined && material.materialLoss !== null) {
  row.materialLoss = material.materialLoss
}
```

---

## 🎯 功能说明

### 为什么使用基础单价？

**基础单价的优势：**

1. **统一单位** - 所有物料都基于基础单位（个、kg等）
2. **便于对比** - 不同采购单位的物料可直接比较
3. **成本准确** - 基于基础单位计算成本更精确
4. **数据一致** - 避免采购单位不同导致的混乱

**示例：**
```
物料A：
  采购单价：100元/箱（1箱=10个）
  基础单价：10元/个

物料B：
  采购单价：12元/个
  基础单价：12元/个

结论：物料A更便宜（10元 < 12元）✅
```

---

### 数据流水线

**选择子件时自动填充的字段：**

1. ✅ **子件编码/名称** - 自动联动
2. ✅ **产出工序** - 从物料库的产出工序名称
3. ✅ **子件来源** - 从物料库的来源（取第一个）
4. ✅ **工序工资** - 从物料库的工序单价
5. ✅ **材料损耗** - 从物料库的材料损耗（新增）
6. ✅ **材料单价** - 从物料库的**基础单价**（已更新）

---

## 📋 使用说明

### 场景1：新增生产BOM

**步骤：**
1. 点击"新增BOM"按钮
2. 填写BOM基本信息
3. 添加子件：
   - 选择子件编码/名称
   - **自动填充材料单价（基础单价）**
   - 填写标准用量
4. 点击"保存"或"提交"
5. **数据保存到数据库** ✅

**特点：**
- 选择子件时，材料单价自动从**基础单价**加载
- 所有数据保存到数据库，**永久保存**
- 重启、更新系统都**不会丢失数据**

---

### 场景2：手动加载材料单价

**步骤：**
1. 打开生产BOM编辑页面
2. 确保计算模式为"手动加载"
3. 添加子件
4. 点击"加载材料单价"按钮
5. **系统从物料库的基础单价批量加载**

**提示：** `已加载 ${count} 条子件的材料单价（基础单价）`

---

### 场景3：编辑现有BOM

**步骤：**
1. 从列表选择BOM，点击"编辑"
2. **系统从数据库加载完整数据**（包含所有子件）
3. 修改数据
4. 点击"保存"或"提交"
5. **更新保存到数据库** ✅

**特点：**
- 所有已加载的数据都**从数据库读取**
- 修改后保存到数据库，**长期保存**
- 不会因为重启、更新而丢失

---

## 🔒 数据安全保障

### 1. 数据库持久化

**保存位置：**
```
data/enterprise_brain.db
```

**特点：**
- ✅ SQLite数据库文件
- ✅ 本地文件存储
- ✅ 断电不丢失
- ✅ 重启自动加载

---

### 2. 自动备份机制

**备份时机：**
- ✅ 每4小时自动备份
- ✅ 每天8:00自动备份
- ✅ 服务启动时立即备份

**备份位置：**
```
data/backups/enterprise_brain_YYYYMMDD_HHMMSS.db
```

**保留策略：**
- ✅ 保留最近30个备份
- ✅ 自动清理旧备份
- ✅ 可手动恢复

---

### 3. 外键约束

**数据完整性：**
```sql
FOREIGN KEY (bom_id) REFERENCES production_boms(id) ON DELETE CASCADE
```

**作用：**
- ✅ 删除BOM时自动删除子件
- ✅ 防止孤立数据
- ✅ 保证数据一致性

---

### 4. 事务处理

**保存逻辑：**
```javascript
// 开始事务
db.exec('BEGIN TRANSACTION');

// 保存BOM主表
// 保存BOM子件

// 提交事务
db.exec('COMMIT');
```

**作用：**
- ✅ 原子性操作
- ✅ 全部成功或全部失败
- ✅ 防止部分保存

---

## 📊 修改的文件

### 前端文件

**`07-frontend/src/pages/bom/ProductionBom.vue`**

**修改内容：**
1. ✅ `handleLoadMaterialPrice` - 从基础单价加载
2. ✅ `handleChildCodeChange` - 选择编码时从基础单价填充
3. ✅ `handleChildNameChange` - 选择名称时从基础单价填充
4. ✅ 增加材料损耗自动填充逻辑

**代码行数：**
- 修改：约30行
- 新增：约10行

---

## 💡 测试建议

### 测试1：数据持久化验证

**步骤：**
1. 新增一个生产BOM
2. 添加子件并保存
3. **关闭系统/重启电脑**
4. 重新打开系统
5. 查看该BOM是否还在
6. 查看子件数据是否完整

**预期结果：** ✅ 所有数据都在，没有丢失

---

### 测试2：基础单价加载验证

**步骤：**
1. 选择一个有基础单价的物料作为子件
2. 观察材料单价是否自动填充
3. 确认填充的是**基础单价**而不是采购单价

**预期结果：** ✅ 材料单价 = 物料库的基础单价

**示例：**
```
物料：60*48面包脚套管/EPE管
  采购单价：0.37元
  采购转化率：1
  基础单价：0.37元

BOM子件材料单价：0.37元 ✅
```

---

### 测试3：手动加载验证

**步骤：**
1. 添加多个子件（不选择物料）
2. 手动输入子件编码
3. 点击"加载材料单价"按钮
4. 观察是否批量加载基础单价

**预期结果：** ✅ 提示"已加载 X 条子件的材料单价（基础单价）"

---

### 测试4：更新验证

**步骤：**
1. 创建一个BOM并保存
2. 修改BOM信息
3. 点击"保存"按钮
4. **不关闭对话框**
5. 再次修改
6. 点击"提交"按钮
7. 关闭后重新打开编辑

**预期结果：** ✅ 所有修改都已保存

---

## ⚠️ 注意事项

### 1. 基础单价必须设置

**问题：** 如果物料库的基础单价为0或未设置，材料单价可能为0

**解决方法：**
1. 到物料库检查物料的采购单价和采购转化率
2. 基础单价会自动计算：`基础单价 = 采购单价 ÷ 采购转化率`
3. 如果基础单价为0，检查采购单价是否正确

---

### 2. 数据加载时机

**自动加载：**
- ✅ 选择子件编码时
- ✅ 选择子件名称时

**手动加载：**
- ✅ 点击"加载材料单价"按钮

---

### 3. 数据保存时机

**重要：** 必须点击"保存"或"提交"按钮才会保存到数据库

**不会自动保存的操作：**
- ❌ 只添加子件没有保存
- ❌ 只修改数据没有点保存
- ❌ 点击"取消"按钮

**建议：** 编辑过程中定期点击"保存"按钮，避免数据丢失

---

## 🎉 更新总结

### 核心改进

1. **数据持久化保障** ✅
   - 所有数据保存到SQLite数据库
   - 支持重启、更新、断电等情况
   - 自动备份机制
   - 外键约束和事务处理

2. **材料单价使用基础单价** ✅
   - 统一单位，便于对比
   - 成本计算更准确
   - 3个地方都已更新（手动加载+2个自动填充）

3. **增强数据流水线** ✅
   - 新增材料损耗自动填充
   - 提示消息更明确

---

### 用户价值

**数据安全：** 不用担心数据丢失  
**成本准确：** 基础单价计算成本更精确  
**操作便捷：** 自动填充节省时间  
**数据一致：** 统一单位避免混乱

---

**生产BOM数据已完全持久化，材料单价已改为基础单价！** 🎊

**测试建议：**
1. 新增BOM → 保存 → 重启系统 → 查看BOM是否还在
2. 选择子件 → 确认材料单价是基础单价
3. 点击加载材料单价 → 确认提示消息包含"基础单价"
