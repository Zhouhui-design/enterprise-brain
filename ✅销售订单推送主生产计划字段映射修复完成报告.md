# ✅ 销售订单推送主生产计划字段映射修复完成报告

## 📋 问题描述

**用户反馈**：

- **销售订单页面**：`http://localhost:3003/sales/orders/list`
- **主生产计划页面**：`http://localhost:3003/production-planning/plan-list`
- **操作**：在销售订单中点击"确认下单"，触发推送到主生产计划和采购计划
- **预期**：主生产计划应该包含以下字段的正确映射：
  1. ❌ 主生产计划的**销售员** = 销售订单中的**销售员**
  2. ❌ 主生产计划的**销售单位** = 销售订单中的**产品单位**
  3. ❌ 主生产计划的**产品来源** = 销售订单中的**产品来源**
  4. ❌ 主生产计划的**提交人** = 销售订单中的**销售员**

**实际情况**：这些字段都没有按预期生成，主生产计划中这些字段为空或默认值

---

## 🔍 问题根源分析

### 代码位置
**文件**：`backend/routes/salesOrders.js`  
**函数**：`POST /confirm-order` (第595-752行)  
**问题区域**：第687-706行

### 原始代码问题

```javascript
// ❌ 原始代码 - 缺少关键字段映射
await connection.execute(`
  INSERT INTO master_production_plans (
    plan_code, product_code, product_name, order_quantity,
    output_process, promised_delivery_date,
    internal_order_no, customer_order_no, customer_name,
    status, submitter
  ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
`, [
  planCode,
  product.product_code,
  product.product_name,
  product.order_quantity,
  outputProcess,
  order.customer_delivery,
  order.internal_order_no,
  order.customer_order_no,
  order.customer_name,
  '已下单',
  'admin'  // ❌ 固定为admin，未使用销售员
]);
```

### 缺少的字段映射

| 主生产计划字段 | 应该映射自 | 原代码 | 修复后 |
| -------------- | ----------- | -------- | -------- |
| `salesperson` | `order.salesperson` | ❌ 缺失 | ✅ 已添加 |
| `sales_unit` | `product.product_unit` | ❌ 缺失 | ✅ 已添加 |
| `product_source` | `product.product_source` | ❌ 缺失 | ✅ 已添加 |
| `submitter` | `order.salesperson` | ❌ 固定'admin' | ✅ 使用销售员 |

---

## 🔧 修复方案

### 修复代码

**文件**：`backend/routes/salesOrders.js`  
**位置**：第680-713行

```
// ✅ 修复后的代码 - 完整字段映射
await connection.execute(`
  INSERT INTO master_production_plans (
    plan_code, product_code, product_name, order_quantity,
    salesperson, sales_unit, product_source,           // 🆕 新增3个字段
    output_process, promised_delivery_date,
    internal_order_no, customer_order_no, customer_name,
    status, submitter, created_at, updated_at           // 🆕 修改submitter
  ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, NOW(), NOW())
`, [
  planCode,
  product.product_code,
  product.product_name,
  product.order_quantity,
  order.salesperson || '',              // ✅ 销售员
  product.product_unit || '',           // ✅ 销售单位（产品单位）
  product.product_source || '',         // ✅ 产品来源
  outputProcess,
  order.customer_delivery,
  order.internal_order_no,
  order.customer_order_no,
  order.customer_name,
  '已下单',
  order.salesperson || 'admin'          // ✅ 提交人使用销售员
]);
```

### 增强返回结果

同时增强了返回数据，方便调试验证：

```javascript
productionPlans.push({
  productCode: product.product_code,
  productName: product.product_name,
  planCode: planCode,
  salesperson: order.salesperson,        // 🆕 新增
  productUnit: product.product_unit,     // 🆕 新增
  productSource: product.product_source  // 🆕 新增
});
```

---

## 📊 字段映射详细说明

### 1. 销售员字段映射

```
销售订单 (sales_orders表)
  └─ salesperson 字段
      ↓
主生产计划 (master_production_plans表)
  └─ salesperson 字段
  └─ submitter 字段（提交人也使用销售员）
```

**代码实现**：
```javascript
salesperson: order.salesperson || '',
submitter: order.salesperson || 'admin'
```

### 2. 产品单位字段映射

```
销售订单产品明细 (sales_order_products表)
  └─ product_unit 字段
      ↓
主生产计划 (master_production_plans表)
  └─ sales_unit 字段（销售单位）
```

**代码实现**：
```javascript
sales_unit: product.product_unit || ''
```

**说明**：主生产计划中的 `sales_unit`（销售单位）实际上就是产品单位

### 3. 产品来源字段映射

```
销售订单产品明细 (sales_order_products表)
  └─ product_source 字段（"自制"或"外购"）
      ↓
主生产计划 (master_production_plans表)
  └─ product_source 字段
```

**代码实现**：
```javascript
product_source: product.product_source || ''
```

**说明**：产品来源从产品手册lookup获取，在销售订单创建时已保存

---

## 🔄 完整数据流

### 确认下单流程

```text
1️⃣ 用户操作
   └─ 销售订单列表 → 选择订单 → 点击"确认下单"

2️⃣ 后端处理 (salesOrders.js confirm-order接口)
   ├─ 获取订单主信息 (order)
   │   └─ salesperson（销售员）
   │   └─ customer_delivery（客户交期）
   │   └─ internal_order_no（内部订单号）
   │   └─ customer_order_no（客户订单号）
   │   └─ customer_name（客户名称）
   │
   ├─ 获取订单产品明细 (products)
   │   └─ product_code（产品编号）
   │   └─ product_name（产品名称）
   │   └─ order_quantity（订单数量）
   │   └─ product_unit（产品单位）✅
   │   └─ product_source（产品来源）✅
   │   └─ output_process（产出工序）
   │
   ├─ 判断产出工序
   │   ├─ output_process = "采购" → 推送到采购计划
   │   └─ output_process ≠ "采购" → 推送到主生产计划 ✅
   │
   └─ 插入主生产计划
       └─ 映射所有字段（包括新增的3个字段）✅

3️⃣ 数据库保存
   └─ master_production_plans 表
       ├─ plan_code（主生产计划编号）
       ├─ product_code（产品编号）
       ├─ product_name（产品名称）
       ├─ order_quantity（订单数量）
       ├─ salesperson（销售员）✅ 新增
       ├─ sales_unit（销售单位）✅ 新增
       ├─ product_source（产品来源）✅ 新增
       ├─ output_process（产出工序）
       ├─ promised_delivery_date（承诺交期）
       ├─ internal_order_no（内部订单号）
       ├─ customer_order_no（客户订单号）
       ├─ customer_name（客户名称）
       ├─ status（状态：已下单）
       └─ submitter（提交人：使用销售员）✅ 修改

4️⃣ 前端显示
   └─ 主生产计划列表
       └─ 所有字段正确显示 ✅
```

---

## 🧪 测试验证步骤

### 准备工作

1. **确保销售订单中有完整数据**
   - 打开销售订单：`http://localhost:3003/sales/orders/list`
   - 新增一个订单，确保填写：
     - 销售员（例如：张三）
     - 产品信息（包括产品单位、产品来源）

### 测试步骤

#### 步骤1：创建销售订单
```text
1. 访问 http://localhost:3003/sales/orders/list
2. 点击"新增"按钮
3. 填写订单信息：
   - 客户名称：测试客户
   - 销售员：张三 ✅
   - 其他必填字段
4. 添加产品：
   - 选择产品编号（会自动lookup产品来源和产出工序）
   - 验证"产品来源"自动填充（例如："自制"）✅
   - 验证"产品单位"显示正确（例如："个"）✅
   - 订单数量：100
5. 点击"提交"
6. 确认订单创建成功
```

#### 步骤2：确认下单
```text
1. 在销售订单列表中找到刚创建的订单
2. 勾选该订单
3. 点击"确认下单"按钮
4. 确认提示对话框中的信息
5. 等待推送完成（查看成功消息）
```

#### 步骤3：验证主生产计划
```text
1. 访问 http://localhost:3003/production-planning/plan-list
2. 找到刚推送的主生产计划（最新的计划）
3. 验证字段：
   ✅ 销售员 = "张三"（应该等于销售订单的销售员）
   ✅ 销售单位 = "个"（应该等于产品单位）
   ✅ 产品来源 = "自制"（应该等于销售订单的产品来源）
   ✅ 提交人 = "张三"（应该等于销售员）
   ✅ 产品编号、产品名称、订单数量等其他字段也正确
```

### 预期结果

**修复前**：
```text
销售员：（空）❌
销售单位：（空）❌
产品来源：（空）❌
提交人：admin ❌
```

**修复后**：
```text
销售员：张三 ✅
销售单位：个 ✅
产品来源：自制 ✅
提交人：张三 ✅
```

---

## 🎯 对比参考

### masterProductionPlans.js（正确示例）

在 `backend/routes/masterProductionPlans.js` 中，执行排程时的字段映射是正确的：

```javascript
// ✅ 正确的字段映射示例（第180-209行）
INSERT INTO master_production_plans (
  plan_code, product_code, product_name, order_quantity,
  salesperson, sales_unit, available_stock, current_stock,
  plan_quantity, product_image, output_process, promised_delivery_date,
  status, planned_storage_date, product_source,
  internal_order_no, customer_order_no,
  customer_name, submitter,
  created_at, updated_at
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, NOW(), NOW())
```

参数映射：
```javascript
[
  planCode,
  product.productCode || '',
  product.productName || '',
  orderQuantity,
  order.salesperson || '',           // ✅ 销售员
  product.productUnit || '',         // ✅ 销售单位
  availableStock,
  0,
  planQuantity,
  productImage,
  product.outputProcess || '',       // ✅ 产出工序
  promisedDeliveryDate,
  '已下单',
  plannedStorageDate,
  productSource,                     // ✅ 产品来源
  order.internalOrderNo || '',
  order.customerOrderNo || '',
  order.customerName || '',
  order.submitter || 'admin'         // ✅ 提交人
]
```

**现在 `salesOrders.js` 中的确认下单功能已经与此保持一致！**

---

## 📝 技术要点

### 1. 字段命名规范

| 前端（驼峰命名） | 数据库（下划线） | 说明 |
|----------------|----------------|------|
| `salesperson` | `salesperson` | 销售员 |
| `productUnit` | `product_unit` | 产品单位 |
| `productSource` | `product_source` | 产品来源 |
| `salesUnit` | `sales_unit` | 销售单位（同产品单位） |

### 2. 空值处理

使用 `||` 运算符提供默认值：
```javascript
order.salesperson || ''        // 如果为null，使用空字符串
product.product_unit || ''
product.product_source || ''
order.salesperson || 'admin'   // 提交人默认为admin
```

### 3. 数据来源链路

```text
产品手册 (product_manual)
  └─ source 字段
      ↓
销售订单产品明细 (sales_order_products)
  └─ product_source 字段
      ↓
主生产计划 (master_production_plans)
  └─ product_source 字段
```

---

## ✅ 修复完成清单

- [x] 添加 `salesperson` 字段映射（从 `order.salesperson`）
- [x] 添加 `sales_unit` 字段映射（从 `product.product_unit`）
- [x] 添加 `product_source` 字段映射（从 `product.product_source`）
- [x] 修改 `submitter` 字段映射（使用 `order.salesperson` 而非固定'admin'）
- [x] 增强返回数据（包含新增字段信息）
- [x] 验证数据库表结构支持这些字段
- [x] 创建详细测试验证步骤

---

## 🎉 预期效果

修复后，从销售订单确认下单推送到主生产计划时：

1. ✅ **销售员字段**：正确映射销售订单的销售员
2. ✅ **销售单位字段**：正确映射产品的单位
3. ✅ **产品来源字段**：正确映射产品来源（"自制"或"外购"）
4. ✅ **提交人字段**：使用销售员而非固定的"admin"
5. ✅ **其他字段**：产品编号、产品名称、订单数量等保持原有正确映射

### 数据完整性保证

- ✅ 销售订单 → 主生产计划：字段完整映射
- ✅ 销售订单 → 采购计划：保持原有逻辑（未修改）
- ✅ 主生产计划 → 工序计划：下游流程不受影响

---

**修复时间**：2025-12-19  
**修复范围**：销售订单确认下单推送主生产计划字段映射  
**影响模块**：销售订单确认下单、主生产计划数据生成  
**测试状态**：待用户验证  
**兼容性**：向后兼容，不影响现有数据
