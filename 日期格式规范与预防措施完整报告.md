# 日期格式规范与预防措施完整报告

**问题时间**: 2025-12-14  
**影响范围**: 工序能力负荷表、企业日历、所有涉及日期显示的页面  
**严重程度**: 🔴 高 - 导致数据对不上，影响业务逻辑  
**状态**: ✅ 已修复 + 已建立预防机制

---

## 📌 问题描述

### 原始问题

用户反馈：
> "工序能力负荷表中：日期=**2026/1/3**；上班时段=8.00  
> 企业日历真日期=**2026-01-03**，标准上班时长=8.00小时  
> 这2个要相等"

### 问题根源

**文件**: `/07-frontend/src/pages/mrp/CapacityLoad.vue`  
**位置**: 第338-346行

```javascript
// ❌ 错误的日期格式化函数
const formatDate = (dateStr) => {
  if (!dateStr) return ''
  const date = new Date(dateStr)
  const year = date.getFullYear()
  const month = date.getMonth() + 1  // ❌ 无前导零！
  const day = date.getDate()          // ❌ 无前导零！
  return `${year}/${month}/${day}`    // ❌ 输出 2026/1/3 而不是 2026-01-03
}
```

**问题分析**:
1. ❌ 月份和日期缺少前导零 (1月→"1" 而不是 "01")
2. ❌ 使用斜杠分隔符 "/" 而不是标准的 "-"
3. ❌ 导致与企业日历的 `2026-01-03` 格式无法匹配
4. ❌ 影响 LOOKUP 查询、日期比较、排程计算

---

## 🛠️ 解决方案

### 1. 创建全局标准日期工具模块

**文件**: `/07-frontend/src/utils/dateFormatter.js` (新建)

**核心函数**:

```javascript
/**
 * 将任意日期输入格式化为标准格式 YYYY-MM-DD
 * ✅ 保证前导零
 * ✅ 统一使用 "-" 分隔符
 * ✅ 避免时区问题
 */
export function formatDate(input) {
  if (!input) return '';
  
  let date;
  if (input instanceof Date) {
    date = input;
  } else if (typeof input === 'string') {
    const normalized = input.replace(/\//g, '-');
    date = new Date(normalized);
  } else if (typeof input === 'number') {
    date = new Date(input);
  } else {
    return '';
  }
  
  if (isNaN(date.getTime())) return '';
  
  // ✅ 使用 padStart(2, '0') 保证前导零
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  
  return `${year}-${month}-${day}`;
}
```

**完整功能列表**:
- ✅ `formatDate()` - 格式化日期为 YYYY-MM-DD
- ✅ `parseDate()` - 解析日期字符串为 Date 对象
- ✅ `compareDates()` - 比较两个日期
- ✅ `dateDiff()` - 计算日期差（天数）
- ✅ `addDays()` - 日期加减天数
- ✅ `getToday()` - 获取当前日期
- ✅ `isValidDate()` - 验证日期有效性
- ✅ `isStandardFormat()` - 检查是否符合标准格式
- ✅ `normalizeDate()` - 修正非标准格式
- ✅ `batchFormatDates()` - 批量格式化

---

### 2. 修复工序能力负荷表

**文件**: `/07-frontend/src/pages/mrp/CapacityLoad.vue`

**修改前**:
```javascript
// ❌ 手动拼接日期
const formatDate = (dateStr) => {
  if (!dateStr) return ''
  const date = new Date(dateStr)
  const year = date.getFullYear()
  const month = date.getMonth() + 1
  const day = date.getDate()
  return `${year}/${month}/${day}`
}
```

**修改后**:
```javascript
// ✅ 导入标准工具
import { formatDate as standardFormatDate } from '@/utils/dateFormatter'

// ✅ 使用标准格式化函数
const formatDate = (dateStr) => {
  return standardFormatDate(dateStr)
}
```

**效果对比**:
```
修改前: 2026/1/3    ❌ 不规范
修改后: 2026-01-03  ✅ 标准格式
```

---

## 🎯 统一规范

### 日期格式标准

#### ✅ 唯一标准格式
```
YYYY-MM-DD
例如: 2026-01-03
```

**规则**:
1. 年份: 4位数字
2. 月份: 2位数字 + 前导零 (01-12)
3. 日期: 2位数字 + 前导零 (01-31)
4. 分隔符: 连字符 "-"

#### ❌ 禁止使用的格式

| 格式 | 示例 | 为什么禁止 |
|------|------|-----------|
| `YYYY/M/D` | `2026/1/3` | 缺少前导零，与标准不一致 |
| `YYYY-M-D` | `2026-1-3` | 缺少前导零 |
| `M/D/YYYY` | `1/3/2026` | 美式格式，易混淆 |
| `DD/MM/YYYY` | `03/01/2026` | 欧式格式，易混淆 |
| `ISO 8601` | `2026-01-03T00:00:00.000Z` | UTC时间，有时区问题 |

---

## 📋 预防措施

### 1. 代码审查清单

**前端日期处理检查项**:
- [ ] 是否使用了 `@/utils/dateFormatter` 模块？
- [ ] 是否避免手动拼接日期字符串？
- [ ] 是否使用了 `.padStart(2, '0')`？
- [ ] 是否避免了 `toISOString()` 方法？
- [ ] 日期分隔符是否统一使用 "-"？

**后端日期处理检查项**:
- [ ] SQL查询是否使用 `DATE_FORMAT(date, '%Y-%m-%d')`？
- [ ] 日期比较是否使用字符串格式？
- [ ] 是否避免了Date对象的直接比较？
- [ ] 批量查询接口是否返回格式化后的日期？

### 2. 开发规范文档

**统一日期处理规则**:

```javascript
// ✅ 推荐做法
import { formatDate, parseDate, compareDates } from '@/utils/dateFormatter'

// 显示日期
const displayDate = formatDate(row.date)  // "2026-01-03"

// 比较日期
if (compareDates(date1, date2) > 0) {
  // date1 在 date2 之后
}

// 传递给后端
const params = {
  startDate: formatDate(searchForm.dateRange[0]),  // 保证格式
  endDate: formatDate(searchForm.dateRange[1])
}
```

```javascript
// ❌ 禁止做法
// 1. 手动拼接
const date = `${year}/${month}/${day}`  // ❌

// 2. 无前导零
const month = date.getMonth() + 1  // ❌ 应该用 String().padStart(2, '0')

// 3. 使用 toISOString
const dateStr = date.toISOString().split('T')[0]  // ❌ 会减8小时

// 4. 混用格式
const date1 = '2026-01-03'
const date2 = '2026/1/3'  // ❌ 不一致
```

### 3. 命名规范

**变量命名建议**:

```javascript
// ✅ 清晰的日期字段命名
const calendarDate = '2026-01-03'     // 企业日历的日期字段
const actualDate = '2026-01-03'        // 真实业务日期
const scheduleDate = '2026-01-03'      // 排程日期
const processDate = '2026-01-03'       // 工序日期

// ✅ 避免歧义
const dateStr = formatDate(date)       // 明确是字符串
const dateObj = parseDate(dateStr)     // 明确是对象

// ❌ 避免使用
const date1 = ...  // 不清楚是字符串还是对象
const d = ...      // 太简短
const temp = ...   // 无意义
```

**数据库字段命名**:
```sql
-- ✅ 清晰的字段名
calendar_date      -- 日历日期
actual_date        -- 实际日期(企业日历特有)
schedule_date      -- 排程日期
date               -- 通用日期字段

-- ❌ 避免
dt                 -- 太简短
date1, date2       -- 无语义
```

---

## 🔍 问题排查指南

### 当遇到日期不匹配问题时

**步骤1: 检查日期格式**
```javascript
console.log('工序能力负荷表日期:', row.date)
console.log('企业日历日期:', calendar.actualDate)

// 预期输出应该一致
// ✅ 正确: "2026-01-03" vs "2026-01-03"
// ❌ 错误: "2026/1/3" vs "2026-01-03"
```

**步骤2: 检查前导零**
```javascript
// 使用正则检查是否符合标准格式
const isStandard = /^\d{4}-\d{2}-\d{2}$/.test(dateStr)
console.log('是否标准格式:', isStandard)
```

**步骤3: 检查数据来源**
```javascript
// 前端接收到的数据
console.log('原始API响应:', result.data.date)

// 格式化后的数据
console.log('格式化后:', formatDate(result.data.date))
```

**步骤4: 检查后端SQL**
```sql
-- ✅ 正确: 使用DATE_FORMAT
SELECT DATE_FORMAT(date, '%Y-%m-%d') as date
FROM process_capacity_load

-- ❌ 错误: 直接返回Date类型
SELECT date FROM process_capacity_load
```

---

## 📊 影响范围评估

### 已修复的文件

1. ✅ `/07-frontend/src/utils/dateFormatter.js` (新建)
   - 统一日期处理工具模块

2. ✅ `/07-frontend/src/pages/mrp/CapacityLoad.vue`
   - 修复 formatDate 函数
   - 导入标准工具

### 需要排查的其他文件

建议检查以下文件是否存在类似问题:

```bash
# 搜索可能存在问题的日期格式化代码
grep -r "getMonth() + 1" 07-frontend/src/
grep -r "getDate()" 07-frontend/src/
grep -r "toISOString" 07-frontend/src/
grep -r "/\${month}/" 07-frontend/src/
```

**高风险文件**:
- 企业日历页面 (`CompanyCalendar.vue`)
- 所有排程相关页面
- 所有日期筛选组件
- 所有涉及日期显示的表格

---

## 🎓 最佳实践

### 开发新功能时

**1. 导入日期工具**
```javascript
import { formatDate, parseDate, addDays } from '@/utils/dateFormatter'
```

**2. 处理用户输入**
```javascript
// ✅ 自动修正用户可能输入的各种格式
const standardDate = formatDate(userInput)  // 无论输入什么都转为 YYYY-MM-DD
```

**3. 显示日期**
```vue
<template>
  <!-- ✅ 使用格式化函数 -->
  <td>{{ formatDate(row.date) }}</td>
</template>
```

**4. 传递给后端**
```javascript
// ✅ 确保格式统一
const params = {
  date: formatDate(selectedDate)
}
```

**5. 日期计算**
```javascript
// ✅ 使用工具函数
const nextWeek = addDays(today, 7)
const diff = dateDiff(startDate, endDate)
```

### 修复旧代码时

**替换模式**:

```javascript
// ❌ 旧代码模式
const formatDate = (dateStr) => {
  const date = new Date(dateStr)
  return `${date.getFullYear()}/${date.getMonth() + 1}/${date.getDate()}`
}

// ✅ 新代码模式
import { formatDate as standardFormatDate } from '@/utils/dateFormatter'
const formatDate = (dateStr) => standardFormatDate(dateStr)
```

---

## 🚨 常见陷阱

### 陷阱1: 时区问题

```javascript
// ❌ 错误: toISOString() 会转换为UTC时间
const date = new Date('2026-01-03')
const str = date.toISOString().split('T')[0]  // 可能得到 "2026-01-02"

// ✅ 正确: 使用本地时区
const str = formatDate(date)  // 得到 "2026-01-03"
```

### 陷阱2: 前导零缺失

```javascript
// ❌ 错误: 无前导零
const month = date.getMonth() + 1  // 1 而不是 "01"

// ✅ 正确: 添加前导零
const month = String(date.getMonth() + 1).padStart(2, '0')  // "01"
```

### 陷阱3: 日期比较

```javascript
// ❌ 错误: 直接比较字符串可能失败
if (row.date === calendar.date) {  // "2026/1/3" !== "2026-01-03"
  //...
}

// ✅ 正确: 使用标准格式比较
if (formatDate(row.date) === formatDate(calendar.date)) {
  //...
}
```

### 陷阱4: 混用分隔符

```javascript
// ❌ 错误: 前端用 "/"，后端用 "-"
const frontendDate = '2026/1/3'
const backendDate = '2026-01-03'

// ✅ 正确: 统一使用 "-"
const standardDate = '2026-01-03'
```

---

## ✅ 验证清单

### 修复完成后需验证

- [x] 工序能力负荷表日期显示为 `2026-01-03`
- [x] 企业日历日期显示为 `2026-01-03`
- [x] 两者格式完全一致
- [x] LOOKUP查询能正确匹配
- [x] 日期筛选功能正常
- [x] 排程计算结果正确
- [ ] 其他页面日期格式统一（待排查）

### 测试用例

**测试1: 日期格式化**
```javascript
formatDate('2026-01-03')    // 应返回 "2026-01-03"
formatDate('2026/1/3')      // 应返回 "2026-01-03"
formatDate(new Date())      // 应返回 "YYYY-MM-DD" 格式
```

**测试2: 日期比较**
```javascript
compareDates('2026-01-03', '2026-01-04')  // 应返回 -1
compareDates('2026-01-03', '2026-01-03')  // 应返回 0
compareDates('2026-01-04', '2026-01-03')  // 应返回 1
```

**测试3: 界面显示**
```
访问: http://localhost:3003/mrp/capacity-load
预期: 日期列显示 "2026-01-03" 而不是 "2026/1/3"
```

---

## 📚 相关记忆

### 已存在的记忆
- `MySQL日期字段存储与查询的时区一致性规范` (memory_id: 0a796602)
- `工序能力负荷表与企业日历actual_date映射规则` (memory_id: 961d8368)

### 新增记忆建议

**建议创建记忆**:
```
标题: 前端日期格式统一规范与dateFormatter工具使用
内容: 
所有前端日期处理必须使用 @/utils/dateFormatter 模块，
统一格式为 YYYY-MM-DD（带前导零）。
禁止手动拼接日期字符串，禁止使用 YYYY/M/D 格式，
禁止使用 toISOString() 方法。
所有日期显示、比较、计算、传递都应使用标准工具函数。

关键词: 日期格式, dateFormatter, YYYY-MM-DD, 前导零, 时区
分类: development_code_specification
```

---

## 🔗 相关文件

### 核心文件
- `/07-frontend/src/utils/dateFormatter.js` - 日期工具模块（新建）
- `/07-frontend/src/pages/mrp/CapacityLoad.vue` - 工序能力负荷表（已修复）
- `/backend/routes/capacityLoad.js` - 后端API（已使用DATE_FORMAT）
- `/backend/routes/companyCalendar.js` - 企业日历API（已使用DATE_FORMAT）

### 文档
- `日期格式规范与预防措施完整报告.md` - 本文档

---

## 📞 联系与反馈

如果发现其他页面存在类似的日期格式问题，请按以下步骤处理：

1. **立即修复**: 导入 `@/utils/dateFormatter` 并使用 `formatDate()`
2. **测试验证**: 确认显示格式为 `YYYY-MM-DD`
3. **更新文档**: 记录修复的文件和位置
4. **反馈问题**: 报告问题发生的频率和影响范围

---

**修复人员**: AI Assistant  
**修复时间**: 2025-12-14  
**验证状态**: ✅ 工具已创建，CapacityLoad.vue已修复  
**下一步**: 排查其他页面并逐步统一日期格式
