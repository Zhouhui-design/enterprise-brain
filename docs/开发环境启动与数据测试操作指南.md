# 开发环境启动与数据测试操作指南

**生成日期**: 2025-12-14  
**适用人员**: 业务用户、测试人员  
**系统**: 企业大脑生产排程系统  

---

## 📋 一、启动命令说明

### 1.1 一键启动(推荐) ⭐

**使用场景**: 每次修改代码后需要重启服务时

**执行命令**:
```bash
cd /home/sardenesy/ai_workspaces/ai_desktop_3
./start-dev.sh
```

**功能**:
- ✅ 自动停止旧服务
- ✅ 启动后端服务(端口3005)
- ✅ 启动前端服务(端口3003)
- ✅ 显示服务状态和访问地址

**输出示例**:
```
🚀 企业大脑系统 - 开发环境启动
=====================================

📋 第1步: 停止旧服务
✅ 旧服务已停止

🔧 第2步: 启动后端服务
✅ 后端服务启动成功 (PID: 149053)
   访问地址: http://localhost:3005

🎨 第3步: 启动前端服务
✅ 前端服务启动成功 (PID: 149067)
   访问地址: http://localhost:3003

✅ 所有服务启动完成！
```

---

### 1.2 手动启动(传统方式)

**场景1: 只启动前端**
```bash
cd /home/sardenesy/ai_workspaces/ai_desktop_3/07-frontend
npm run dev
```
- 访问地址: http://localhost:3003

**场景2: 只启动后端**
```bash
cd /home/sardenesy/ai_workspaces/ai_desktop_3
node backend/server.js
```
- 访问地址: http://localhost:3005

---

### 1.3 查看服务日志

**后端日志**:
```bash
tail -f /home/sardenesy/ai_workspaces/ai_desktop_3/backend-dev.log
```

**前端日志**:
```bash
tail -f /home/sardenesy/ai_workspaces/ai_desktop_3/frontend-dev.log
```

---

### 1.4 停止服务

**停止所有服务**:
```bash
# 使用启动脚本输出的PID
kill 149067 149053  # 替换为实际PID
```

**或者使用端口查杀**:
```bash
# 停止前端(端口3003)
lsof -ti:3003 | xargs kill -9

# 停止后端(端口3005)
lsof -ti:3005 | xargs kill -9
```

---

## 🧪 二、业务数据测试流程

### 2.1 重要原则

**✅ 正确做法**:
1. **通过前端页面操作** - 创建销售订单、执行排程
2. **模拟真实业务流程** - 按照实际操作顺序进行
3. **验证数据流闭环** - 主生产计划 → 备料计划 → 真工序计划

**❌ 禁止做法**:
1. ~~通过数据库后台直接修改数据~~
2. ~~跳过业务流程直接插入数据~~
3. ~~修改已生成的计划数据~~

**例外**: 只有**无法删除的脏数据**才允许通过后端数据库清理

---

### 2.2 标准测试流程

#### 第1步: 创建销售订单

1. 打开前端页面: http://localhost:3003
2. 进入"销售管理" → "销售订单"
3. 点击"新增订单"
4. 填写订单信息:
   - 客户名称: (选择已有客户)
   - 客户订单号: 自定义
   - 承诺交期: 选择日期
   - 产品编号: (选择已有产品)
   - 订单数量: 输入数量
5. 保存订单

**预期结果**: ✅ 订单创建成功,显示在订单列表中

---

#### 第2步: 执行排程

1. 在"生产计划" → "主生产计划"页面
2. 找到刚创建的订单记录
3. 点击"执行排程"按钮

**预期结果**: 
- ✅ 自动创建备料计划
- ✅ 自动创建真工序计划
- ✅ 弹出成功提示

---

#### 第3步: 验证备料计划

1. 进入"生产计划" → "备料计划"页面
2. 查看是否生成了对应的备料计划记录
3. 检查关键字段:
   - 来源主计划编号: 应该是主生产计划编号
   - 需求数量: 根据BOM计算
   - 可用库存: 从库存表查询
   - 需补货数量: 需求数量 - 可用库存

**预期结果**: ✅ 备料计划数据正确,字段计算准确

---

#### 第4步: 验证真工序计划

1. 进入"生产计划" → "真工序计划"页面
2. 查看是否生成了对应的真工序计划记录
3. **关键验证点** - 当天总工时:
   - 工序名称: 如"组装"
   - 计划排程日期: 如"2026-01-05"
   - **当天总工时(daily_total_hours)**: 应该是 **16** (班次8小时 × 工位数2)
   - 计划排程工时: 根据需求工时分配
   - 计划排程数量: 根据定时工额计算

**预期结果**: ✅ 真工序计划数据正确,**当天总工时字段已正确生成**

---

### 2.3 验证数据流闭环

**完整闭环路径**:
```
销售订单
    ↓ 点击"执行排程"
主生产计划
    ↓ 自动创建
备料计划 (物料来源=自制, 需补货数量>0)
    ↓ 自动推送
真工序计划 (计划排程数量>0)
    ↓ 自动推送
备料计划 (更新或新增)
```

**验证方法**:
```sql
-- 1. 查询主生产计划
SELECT plan_code, product_code, plan_quantity 
FROM master_production_plans 
WHERE plan_code = 'YOUR_PLAN_CODE';

-- 2. 查询备料计划
SELECT plan_no, source_plan_no, material_code, demand_quantity, 
       available_stock, replenishment_quantity
FROM material_preparation_plans 
WHERE source_plan_no = 'YOUR_PLAN_CODE';

-- 3. 查询真工序计划
SELECT plan_no, process_name, schedule_date, 
       daily_total_hours, scheduled_work_hours, schedule_quantity
FROM real_process_plans 
WHERE master_plan_no = 'YOUR_PLAN_CODE';
```

---

## 🔍 三、问题排查指南

### 3.1 当天总工时为0的情况

**问题表现**:
真工序计划的`daily_total_hours`字段显示0.00

**可能原因**:
1. ❌ **使用了修复前创建的旧数据**
   - 解决方案: 删除旧记录,通过业务流程重新创建

2. ❌ **工序能力负荷表无对应记录**
   - 检查SQL:
     ```sql
     SELECT process_name, date, work_shift, available_workstations
     FROM process_capacity_load 
     WHERE process_name = '组装' AND date = '2026-01-05';
     ```
   - 解决方案: 确保工序能力负荷表有对应的工序和日期记录

3. ❌ **scheduleDate为null**
   - 检查SQL:
     ```sql
     SELECT plan_no, schedule_date 
     FROM real_process_plans 
     WHERE plan_no = 'RPP2025559034572';
     ```
   - 解决方案: 检查产品物料库是否有定时工额数据

---

### 3.2 示例:RPP2025559034572为什么当天总工时为0

**数据分析**:
```sql
SELECT plan_no, created_at, daily_total_hours
FROM real_process_plans 
WHERE plan_no = 'RPP2025559034572';

-- 结果:
-- plan_no: RPP2025559034572
-- created_at: 2025-12-14 12:45:59   ⬅️ 修复前创建
-- daily_total_hours: 0.00
```

**原因**: 
这条记录的创建时间是`2025-12-14 12:45:59`,在当天总工时修复代码提交之前(12:48)创建的。

**解决方案**:
1. **删除旧记录** (通过数据库或前端页面)
2. **重新创建销售订单**
3. **执行排程**,生成新的真工序计划
4. **验证** `daily_total_hours`字段是否为16

---

### 3.3 浏览器缓存清除

**问题**: 修改代码后,前端页面没有更新

**解决方案**:

**方法1: 硬刷新(推荐)**
- Windows/Linux: `Ctrl + F5`
- Mac: `Cmd + Shift + R`

**方法2: 清除缓存**
- Windows/Linux: `Ctrl + Shift + Delete`
- Mac: `Cmd + Shift + Delete`
- 选择"缓存的图像和文件"
- 点击"清除数据"

**方法3: 重启服务(最彻底)**
```bash
cd /home/sardenesy/ai_workspaces/ai_desktop_3
./start-dev.sh
```

---

## 📝 四、测试场景示例

### 场景1: 完整业务流程测试

**目标**: 验证当天总工时修复是否生效

**步骤**:
1. ✅ 启动服务: `./start-dev.sh`
2. ✅ 浏览器硬刷新: `Ctrl + F5`
3. ✅ 创建销售订单:
   - 产品: 选择有BOM的产品
   - 数量: 50
   - 交期: 2026-01-10
4. ✅ 执行排程
5. ✅ 检查真工序计划:
   ```sql
   SELECT plan_no, process_name, schedule_date, daily_total_hours
   FROM real_process_plans 
   WHERE created_at > '2025-12-14 12:48:00'  -- 修复后创建
   ORDER BY created_at DESC 
   LIMIT 1;
   ```
6. ✅ 验证结果:
   - `daily_total_hours` = 16 (组装工序,8小时×2工位)
   - `schedule_date` 有值
   - `scheduled_work_hours` > 0

**预期输出**:
```
plan_no: RPP2025XXXXXXXXX
process_name: 组装
schedule_date: 2026-01-05
daily_total_hours: 16.00   ✅ 正确!
scheduled_work_hours: 7.00
schedule_quantity: 70.00
```

---

### 场景2: 清理脏数据

**问题**: 测试过程中产生了错误数据

**清理SQL**:
```sql
-- 1. 查看待清理的数据
SELECT plan_no, created_at 
FROM real_process_plans 
WHERE plan_no LIKE 'RPP2025%' 
  AND daily_total_hours = 0 
  AND created_at < '2025-12-14 12:48:00';

-- 2. 删除旧数据(确认无误后执行)
DELETE FROM real_process_plans 
WHERE plan_no LIKE 'RPP2025%' 
  AND daily_total_hours = 0 
  AND created_at < '2025-12-14 12:48:00';

-- 3. 删除对应的备料计划
DELETE FROM material_preparation_plans 
WHERE plan_no IN (
  SELECT source_no FROM real_process_plans 
  WHERE created_at < '2025-12-14 12:48:00'
);
```

**⚠️ 注意**: 只在测试环境执行删除操作!

---

## 🎯 五、快速参考

### 5.1 常用命令

| 操作 | 命令 |
|------|------|
| 一键启动 | `./start-dev.sh` |
| 查看后端日志 | `tail -f backend-dev.log` |
| 查看前端日志 | `tail -f frontend-dev.log` |
| 停止所有服务 | `lsof -ti:3003,3005 \| xargs kill -9` |
| 浏览器硬刷新 | `Ctrl + F5` |
| 清除浏览器缓存 | `Ctrl + Shift + Delete` |

---

### 5.2 关键字段说明

| 表名 | 字段名 | 含义 | 计算公式 |
|------|--------|------|----------|
| real_process_plans | daily_total_hours | 当天总工时 | work_shift × available_workstations |
| real_process_plans | scheduled_work_hours | 计划排程工时 | 需求工时按日期分配 |
| real_process_plans | schedule_quantity | 计划排程数量 | scheduled_work_hours × standard_work_quota |
| process_capacity_load | total_hours | 总工时 | work_shift × available_workstations |
| process_capacity_load | remaining_hours | 剩余工时 | total_hours - occupied_hours |

---

### 5.3 关键API端点

| 功能 | 方法 | 端点 |
|------|------|------|
| 执行排程 | POST | `/api/master-production-plans/:id/execute-schedule` |
| 获取备料计划 | GET | `/api/material-preparation-plans` |
| 获取真工序计划 | GET | `/api/real-process-plans` |
| 获取工序能力负荷 | GET | `/api/capacity-load` |

---

## ⚠️ 六、重要提醒

### 6.1 数据测试规范

1. ✅ **始终使用业务流程创建数据**
2. ✅ **修改代码后必须重启服务**
3. ✅ **刷新浏览器缓存**
4. ✅ **验证数据流闭环**

### 6.2 禁止操作

1. ❌ **不要直接在数据库修改业务数据**
2. ❌ **不要跳过业务流程插入数据**
3. ❌ **不要使用旧数据验证新功能**

### 6.3 修复后的RPP2025559034572

**问题**: 当天总工时为0

**原因**: 该记录创建于修复前(12:45:59)

**解决方案**:
1. 删除该记录(通过SQL或前端)
2. 通过销售订单重新创建
3. 执行排程
4. 验证新记录的`daily_total_hours`字段

**验证新记录**:
```sql
SELECT plan_no, process_name, schedule_date, 
       daily_total_hours, created_at
FROM real_process_plans 
WHERE created_at > '2025-12-14 12:48:00'
ORDER BY created_at DESC;
```

---

## 📞 七、技术支持

**遇到问题时的检查顺序**:
1. ✅ 检查服务是否正常运行
2. ✅ 检查后端日志: `tail -f backend-dev.log`
3. ✅ 检查前端日志: `tail -f frontend-dev.log`
4. ✅ 检查数据库数据是否正确
5. ✅ 检查浏览器控制台错误(F12)

**日志位置**:
- 后端: `/home/sardenesy/ai_workspaces/ai_desktop_3/backend-dev.log`
- 前端: `/home/sardenesy/ai_workspaces/ai_desktop_3/frontend-dev.log`

---

**文档版本**: v1.0  
**最后更新**: 2025-12-14  
**适用系统**: 企业大脑生产排程系统
