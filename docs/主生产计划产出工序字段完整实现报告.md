# 主生产计划产出工序字段完整实现报告

## 📋 需求概述

在主生产计划页面主表格新增"产出工序"字段，并在销售订单正式下单推送到主生产计划时，实现产出工序字段的数据映射。

**数据流向**：
```
销售订单列表"产出工序"（outputProcess）
    ↓ 正式下单推送
主生产计划"产出工序"（outputProcess）
```

---

## 🔧 实施内容

### 1️⃣ 前端修改

#### ✅ 主生产计划列表页面（ProductionPlanList.vue）

**文件**: `/07-frontend/src/pages/production-planning/ProductionPlanList.vue`

**修改位置**: 第232-243行（表格列配置）

**修改内容**: 在"产品图片"列后添加"产出工序"列

```javascript
{
  prop: 'productImage',
  label: '产品图片',
  width: 100,
  slot: 'productImage'
},
{
  prop: 'outputProcess',           // ✅ 新增
  label: '产出工序',                // ✅ 新增
  width: 120,                       // ✅ 新增
  filterable: true,                 // ✅ 新增
  showOverflowTooltip: true         // ✅ 新增
},                                   // ✅ 新增
{
  prop: 'promisedDeliveryDate',
  label: '订单承诺交期',
  width: 150,
  sortable: true
},
```

**效果**: 主生产计划列表表格将在"产品图片"列后显示"产出工序"列。

---

#### ✅ 销售订单列表页面（SalesOrderListNew.vue）

**文件**: `/07-frontend/src/pages/sales/sales-order/SalesOrderListNew.vue`

**修改位置1**: 第1225-1234行（解析JSON产品列表）

**修改内容**: 添加outputProcess字段映射

```javascript
products = parsedProducts.map(p => ({
  productCode: p.product_code || p.productCode,
  productName: p.product_name || p.productName,
  orderQuantity: p.order_quantity || p.orderQuantity,
  productUnit: p.product_unit || p.productUnit,
  productImage: p.product_image || p.productImage,
  outputProcess: p.output_process || p.outputProcess || '' // ✅ 添加产出工序字段
}))
```

**修改位置2**: 第1239-1247行（处理数组产品列表）

```javascript
products = order.productList.map(p => ({
  productCode: p.product_code || p.productCode,
  productName: p.product_name || p.productName,
  orderQuantity: p.order_quantity || p.orderQuantity,
  productUnit: p.product_unit || p.productUnit,
  productImage: p.product_image || p.productImage,
  outputProcess: p.output_process || p.outputProcess || '' // ✅ 添加产出工序字段
}))
```

**修改位置3**: 第1250-1259行（处理单产品订单）

```javascript
if (products.length === 0 && order.productCode) {
  products = [{
    productCode: order.productCode,
    productName: order.productName,
    orderQuantity: order.orderQuantity,
    productUnit: order.productUnit,
    productImage: order.productImage,
    outputProcess: order.outputProcess || '' // ✅ 添加产出工序字段
  }]
}
```

**效果**: 正式下单时，前端会将订单的产出工序字段包含在推送数据中。

---

### 2️⃣ 后端修改

#### ✅ 数据库表结构修改

**文件**: `/backend/migrations/add_output_process_to_master_production_plans.sql`

**执行语句**:
```sql
ALTER TABLE master_production_plans 
ADD COLUMN output_process VARCHAR(100) 
COMMENT '产出工序' 
AFTER product_image;
```

**执行结果**:
```
✅ 字段添加成功
📋 字段信息: {
  "COLUMN_NAME": "output_process",
  "COLUMN_TYPE": "varchar(100)",
  "COLUMN_COMMENT": "产出工序"
}
```

---

#### ✅ 后端API修改（masterProductionPlans.js）

**文件**: `/backend/routes/masterProductionPlans.js`

**修改位置1**: 第114-150行（创建主生产计划）

**修改内容**: 
1. 添加outputProcess到日志输出
2. INSERT语句添加output_process字段
3. VALUES添加product.outputProcess参数

```javascript
console.log('📝 创建主生产计划:', {
  planCode,
  productCode: product.productCode,
  productName: product.productName,
  orderQuantity,
  productImage,
  productSource,
  outputProcess: product.outputProcess || '', // ✅ 添加产出工序
  promisedDeliveryDate
});

const [result] = await connection.execute(`
  INSERT INTO master_production_plans (
    plan_code, product_code, product_name, order_quantity,
    salesperson, sales_unit, available_stock, current_stock,
    plan_quantity, product_image, output_process, promised_delivery_date, // ✅ 添加output_process
    status, planned_storage_date, product_source,
    internal_order_no, customer_order_no,
    created_at, updated_at
  ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, NOW(), NOW()) // ✅ 17个占位符
`, [
  planCode,
  product.productCode || '',
  product.productName || '',
  orderQuantity,
  order.salesperson || '',
  product.productUnit || '',
  availableStock,
  0, // 实时库存暂为0
  planQuantity,
  productImage, // 使用lookup后的图片
  product.outputProcess || '', // ✅ 保存产出工序（从订单获取）
  promisedDeliveryDate,
  '已下单',
  null, // 计划入库日期暂为空
  productSource, // 使用lookup后的产品来源
  order.internalOrderNo || '',
  order.customerOrderNo || ''
]);
```

**修改位置2**: 第191-206行（查询主生产计划列表）

**修改内容**: SELECT语句添加output_process字段并映射为outputProcess

```javascript
let sql = `
  SELECT 
    id, plan_code as planCode, product_code as productCode,
    product_name as productName, order_quantity as orderQuantity,
    salesperson, sales_unit as salesUnit,
    available_stock as availableStock, current_stock as currentStock,
    plan_quantity as planQuantity, product_image as productImage,
    output_process as outputProcess, // ✅ 添加产出工序字段
    promised_delivery_date as promisedDeliveryDate, status,
    planned_storage_date as plannedStorageDate,
    product_source as productSource,
    internal_order_no as internalOrderNo,
    customer_order_no as customerOrderNo,
    created_at as createdAt, updated_at as updatedAt
  FROM master_production_plans
  WHERE 1=1
`;
```

---

## 📊 数据库表结构

### master_production_plans 表（新增字段）

| 字段名 | 类型 | 说明 | 位置 |
|--------|------|------|------|
| product_image | VARCHAR(500) | 产品图片 | - |
| **output_process** | **VARCHAR(100)** | **产出工序** | ✅ **新增** |
| promised_delivery_date | DATE | 订单承诺交期 | - |

---

## 🔄 完整数据流

### 阶段1：新增订单时

```
用户在新增订单页面输入产品编码
    ↓
前端SalesOrderCreate.vue执行lookup
    ↓
调用产品手册API: /api/product-manual
    ↓
获取产出工序名称
    ↓
填充到子表格"产出工序"字段
    ↓
提交订单时保存到sales_order_products表（output_process字段）
```

### 阶段2：正式下单时

```
用户在订单列表选择订单
    ↓
点击"正式下单"按钮
    ↓
前端读取订单的产出工序字段（order.outputProcess）
    ↓
构造产品数据，包含outputProcess字段
    ↓
POST /api/master-production-plans/from-sales-order
    ↓
后端接收product.outputProcess
    ↓
INSERT INTO master_production_plans (output_process字段)
    ↓
保存到数据库
```

### 阶段3：主生产计划列表显示

```
用户访问主生产计划列表页面
    ↓
前端调用API: GET /api/master-production-plans
    ↓
后端查询: SELECT output_process as outputProcess
    ↓
返回包含outputProcess的数据
    ↓
前端表格显示"产出工序"列
```

---

## 📝 字段映射规范

### 前端（camelCase）
- **销售订单**: `outputProcess`
- **主生产计划**: `outputProcess`

### 后端（snake_case）
- **sales_order_products表**: `output_process`
- **master_production_plans表**: `output_process`

### API响应（camelCase）
- **查询订单**: `outputProcess`
- **查询主生产计划**: `outputProcess`

---

## 🧪 测试验证

### 测试步骤

1. **清空浏览器缓存并刷新**
2. **测试订单创建流程**：
   - 访问新增订单页面
   - 输入产品编码（如：6002A0203）
   - 确认产出工序自动填充（如：打包）
   - 提交订单

3. **测试正式下单流程**：
   - 访问订单列表页面
   - 选择一个订单
   - 点击"正式下单"按钮
   - 确认提示信息并确定

4. **验证主生产计划**：
   - 访问主生产计划列表页面
   - 确认新创建的主生产计划
   - 查看"产出工序"列是否正确显示

### 预期结果

✅ **订单列表**：
- 产出工序列正常显示
- 值来自sales_order_products.output_process

✅ **正式下单**：
- 控制台日志显示：
  ```javascript
  📝 创建主生产计划: {
    planCode: "MPS2025120700XX",
    productCode: "6002A0203",
    productName: "产品名称",
    outputProcess: "打包", // ✅ 包含产出工序
    ...
  }
  ```

✅ **主生产计划列表**：
- "产出工序"列显示在"产品图片"后
- 值与订单的产出工序一致
- 支持筛选功能

---

## 🎯 关键改进点

### 1. 数据完整性
- **之前**: 主生产计划缺少产出工序字段
- **现在**: 完整保存产出工序信息
- **优势**: 数据完整，支持后续BOM树生成

### 2. 字段映射一致性
| 数据源 | 前端字段名 | 后端字段名 | 映射关系 |
|--------|-----------|-----------|---------|
| 产品手册 | outputProcess | output_process_name | 源数据 |
| 销售订单产品表 | outputProcess | output_process | 保存订单时映射 |
| 主生产计划表 | outputProcess | output_process | 正式下单时映射 |

### 3. 业务流程规范
- **订单→计划**: 产出工序随订单推送到主生产计划
- **计划→排程**: 产出工序用于工序计划和BOM树生成
- **全流程追溯**: 从订单到生产的完整数据链

---

## 📂 修改文件清单

### 前端文件
- ✅ `/07-frontend/src/pages/production-planning/ProductionPlanList.vue` - 添加产出工序列
- ✅ `/07-frontend/src/pages/sales/sales-order/SalesOrderListNew.vue` - 正式下单时映射产出工序

### 后端文件
- ✅ `/backend/routes/masterProductionPlans.js` - INSERT和SELECT添加output_process字段
- ✅ `/backend/migrations/add_output_process_to_master_production_plans.sql` - 数据库迁移脚本

### 数据库
- ✅ `master_production_plans` 表 - 添加output_process字段（VARCHAR(100)）

### 文档
- ✅ `/docs/主生产计划产出工序字段完整实现报告.md` - 本文档

---

## ✅ 修复完成清单

- [x] 主生产计划表格添加"产出工序"列
- [x] 数据库添加output_process字段
- [x] 后端INSERT语句添加output_process
- [x] 后端SELECT查询添加output_process
- [x] 前端正式下单时映射outputProcess（JSON解析）
- [x] 前端正式下单时映射outputProcess（数组处理）
- [x] 前端正式下单时映射outputProcess（单产品）
- [x] 重启后端服务
- [x] 创建修复文档

---

## 🚀 后续工作

### 建议测试场景

1. **单产品订单正式下单**
   - 订单只有1个产品
   - 验证主生产计划产出工序正确

2. **多产品订单正式下单**
   - 订单有2个及以上产品
   - 验证每个产品对应的主生产计划产出工序正确

3. **产出工序为空的订单**
   - 测试边界情况
   - 验证不会出现错误

### 下游功能集成

根据记忆中的数据流规则，产出工序字段将用于：

1. **执行排程**: 主生产计划 → 备料计划
   - 需要验证产出工序是否正确推送

2. **BOM树生成**: 
   - 产出工序字段应映射到BOM树的相关节点

3. **工序计划**:
   - 根据产出工序自动创建工序计划

---

## 📞 技术支持

### 问题排查

1. **主生产计划不显示产出工序**
   - 检查数据库字段是否存在
   - 检查后端SELECT是否包含output_process
   - 检查前端列配置是否正确

2. **产出工序值为空**
   - 检查订单是否有产出工序
   - 检查前端正式下单时是否映射outputProcess
   - 检查后端INSERT是否保存product.outputProcess

3. **后端错误**
   - 查看backend.log日志
   - 检查INSERT语句占位符数量是否匹配

### 数据库查询验证

```sql
-- 查看主生产计划的产出工序
SELECT 
  plan_code, 
  product_code, 
  product_name, 
  output_process,
  internal_order_no
FROM master_production_plans
ORDER BY created_at DESC
LIMIT 10;

-- 检查字段是否存在
DESCRIBE master_production_plans;
```

---

**实施时间**: 2025-12-07 14:55  
**实施人员**: AI Assistant  
**影响范围**: 主生产计划列表显示、正式下单数据流水线  
**风险等级**: 低（新增字段，不影响现有功能）  
**测试状态**: 待用户验证
