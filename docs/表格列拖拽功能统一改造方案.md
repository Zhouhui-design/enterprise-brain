# 🎯 表格列拖拽功能统一改造方案

## 📋 问题分析

### 当前状况
- ✅ **已实现列拖拽**：`/human-resources/employee-list` (员工台账)
- ❌ **未实现列拖拽**：其他所有表格页面
- ⚠️ **拖拽失效问题**：主生产计划、备料计划、工序计划、预计结存等页面的列设置弹窗中拖拽无反应

### 核心原因分析

#### 1. **组件依赖问题**
主生产计划等页面使用的列设置组件**缺少 `vuedraggable` 依赖**：
```vue
<!-- PageSettings.vue 中缺少拖拽功能 -->
<el-checkbox-group v-model="localVisibleFields">
  <!-- ❌ 没有使用 draggable 组件 -->
  <el-checkbox v-for="field in availableFields" ...>
</el-checkbox-group>
```

对比员工台账使用的 `TableColumnControl` 组件：
```vue
<!-- TableColumnControl/index.vue - 正确实现 -->
<draggable
  v-model="localColumns"
  item-key="prop"
  handle=".drag-handle"
  @end="handleDragEnd"
>
  <template #item="{ element }">
    <div class="column-item">
      <el-icon class="drag-handle"><Rank /></el-icon>
      <el-checkbox v-model="element.visible" />
    </div>
  </template>
</draggable>
```

#### 2. **组件架构不统一**
- **员工台账**：使用 `TableColumnControl` 组件（支持拖拽）
- **主生产计划等**：使用 `PageSettings` 组件（不支持拖拽）
- **其他页面**：未集成列设置功能

#### 3. **代码结构差异**
```
员工台账 ✅
  └─ TableColumnControl (独立组件，支持拖拽)

主生产计划等 ⚠️
  └─ PageSettings (通用设置组件，字段管理是其中一个Tab)
      └─ 字段管理Tab (简单的checkbox，无拖拽)

其他页面 ❌
  └─ 未集成任何列设置功能
```

---

## 🎯 解决方案

### 方案A：最小改动方案（推荐）⭐

**核心思路**：为 `PageSettings` 组件添加拖拽功能，保持现有架构不变。

#### 优点
- ✅ 改动最小，风险最低
- ✅ 不影响现有页面
- ✅ 统一使用 PageSettings 组件
- ✅ 一次修改，所有页面受益

#### 实施步骤

##### 1. 修改 `PageSettings.vue` 组件
**文件**: `07-frontend/src/components/common/PageSettings.vue`

```vue
<template>
  <el-dialog v-model="visible" title="页面设置" width="800px">
    <el-tabs v-model="activeTab">
      <!-- 字段管理Tab -->
      <el-tab-pane label="字段管理" name="fields" v-if="showFields">
        <div class="fields-panel">
          <div class="panel-header">
            <span>拖拽调整字段顺序，勾选控制显示/隐藏</span>
            <el-button type="primary" link size="small" @click="resetFields">重置</el-button>
          </div>
          
          <el-divider style="margin: 10px 0;" />
          
          <!-- ✅ 使用 draggable 替换原有的 checkbox-group -->
          <div class="column-list">
            <draggable
              v-model="localVisibleFields"
              item-key="prop"
              handle=".drag-handle"
              @end="handleFieldDragEnd"
            >
              <template #item="{ element }">
                <div class="field-item">
                  <div class="field-item-left">
                    <el-icon class="drag-handle"><Rank /></el-icon>
                    <el-checkbox
                      :model-value="element.visible"
                      @update:model-value="(val) => updateFieldVisibility(element, val)"
                      :label="element.label"
                    />
                  </div>
                  <div class="field-item-right">
                    <el-tag v-if="element.fixed" size="small" type="info">固定</el-tag>
                  </div>
                </div>
              </template>
            </draggable>
          </div>
        </div>
      </el-tab-pane>
      
      <!-- 其他Tab保持不变 -->
      <el-tab-pane label="业务变量" name="variables" v-if="showBusinessVars">
        <!-- ... existing code ... -->
      </el-tab-pane>
    </el-tabs>
    
    <template #footer>
      <el-button @click="handleCancel">取消</el-button>
      <el-button type="primary" @click="handleSave">保存</el-button>
    </template>
  </el-dialog>
</template>

<script setup>
import { ref, watch, computed } from 'vue'
import { Rank } from '@element-plus/icons-vue'
import draggable from 'vuedraggable'

const props = defineProps({
  modelValue: Boolean,
  availableFields: {
    type: Array,
    default: () => []
  },
  visibleFields: {
    type: Array,
    default: () => []
  },
  showFields: {
    type: Boolean,
    default: true
  },
  // ... 其他props
})

const emit = defineEmits(['update:modelValue', 'save', 'update:visibleFields'])

// ✅ 新增：字段数组（包含visible属性）
const localVisibleFields = ref([])
const originalFields = ref([])

// ✅ 初始化字段列表
const initFields = () => {
  if (props.availableFields && props.availableFields.length > 0) {
    localVisibleFields.value = props.availableFields.map(field => ({
      ...field,
      visible: props.visibleFields ? props.visibleFields.includes(field.prop) : true
    }))
    originalFields.value = JSON.parse(JSON.stringify(localVisibleFields.value))
  }
}

// ✅ 监听字段变化
watch(() => [props.availableFields, props.visibleFields], () => {
  initFields()
}, { immediate: true, deep: true })

// ✅ 更新字段可见性
const updateFieldVisibility = (field, visible) => {
  field.visible = visible
}

// ✅ 拖拽结束事件
const handleFieldDragEnd = () => {
  console.log('✅ 字段顺序已更新:', localVisibleFields.value.map(f => f.prop))
}

// ✅ 重置字段
const resetFields = () => {
  localVisibleFields.value = JSON.parse(JSON.stringify(originalFields.value))
}

// ✅ 保存设置
const handleSave = () => {
  const settings = {
    // 字段顺序和可见性
    fields: localVisibleFields.value,
    visibleFields: localVisibleFields.value.filter(f => f.visible).map(f => f.prop),
    // ... 其他设置
  }
  
  emit('save', settings)
  emit('update:visibleFields', settings.visibleFields)
  visible.value = false
}

</script>

<style scoped>
.fields-panel {
  padding: 10px;
}

.panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-weight: 600;
  color: #303133;
  margin-bottom: 10px;
}

.column-list {
  max-height: 400px;
  overflow-y: auto;
}

.field-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8px 5px;
  border-radius: 4px;
  cursor: move;
  transition: background-color 0.2s;
}

.field-item:hover {
  background-color: #f5f7fa;
}

.field-item-left {
  display: flex;
  align-items: center;
  gap: 8px;
  flex: 1;
}

.drag-handle {
  cursor: move;
  color: #909399;
  font-size: 16px;
}

.drag-handle:hover {
  color: #409eff;
}

.field-item-right {
  margin-left: 10px;
}

.column-list :deep(.el-checkbox) {
  width: 100%;
}

.column-list :deep(.el-checkbox__label) {
  color: #606266;
  font-size: 14px;
}
</style>
```

##### 2. 安装 vuedraggable 依赖（如果尚未安装）
```bash
cd 07-frontend
npm install vuedraggable@next
```

##### 3. 页面端接收字段顺序更新
**示例：主生产计划**

```javascript
// ProductionPlanList.vue
const handleSettingsSave = (settings) => {
  console.log('✅ 设置已保存:', settings)
  
  // ✅ 应用字段顺序
  if (settings.fields) {
    tableColumns.value = settings.fields.map(field => {
      const originalColumn = tableColumns.value.find(col => col.prop === field.prop)
      return {
        ...originalColumn,
        visible: field.visible
      }
    })
  }
  
  // ✅ 应用字段可见性
  if (settings.visibleFields) {
    // 更新可见字段列表
  }
  
  this.$message.success('设置已应用')
}
```

---

### 方案B：创建统一表格组件（长期方案）

**核心思路**：创建一个 `StandardTablePage` 组件，内置所有功能。

#### 优点
- ✅ 统一架构，易于维护
- ✅ 新页面开发快速
- ✅ 功能完整且统一

#### 缺点
- ⚠️ 需要重构现有页面
- ⚠️ 短期工作量大

#### 组件结构
```vue
<template>
  <div class="standard-table-page">
    <!-- 顶部工具栏 -->
    <div class="page-header">
      <h2>{{ pageTitle }}</h2>
      <div class="header-actions">
        <el-button type="primary" @click="handleAdd">新增</el-button>
        <!-- ... 其他按钮 -->
        <TableColumnControl
          :columns="columns"
          @update:columns="handleColumnsUpdate"
        />
      </div>
    </div>
    
    <!-- 搜索区 -->
    <el-card class="search-card" v-if="showSearch">
      <slot name="search-form"></slot>
    </el-card>
    
    <!-- 表格 -->
    <el-card class="table-card">
      <EnhancedTable
        :data="tableData"
        :columns="visibleColumns"
        :loading="loading"
        :show-filter="true"
        :show-pagination="true"
        :total="total"
        @selection-change="handleSelectionChange"
      >
        <slot></slot>
      </EnhancedTable>
    </el-card>
  </div>
</template>

<script setup>
import EnhancedTable from '@/components/common/EnhancedTable.vue'
import TableColumnControl from '@/components/TableColumnControl/index.vue'
import { ref, computed } from 'vue'

const props = defineProps({
  pageTitle: String,
  columns: Array,
  tableData: Array,
  loading: Boolean,
  total: Number,
  showSearch: {
    type: Boolean,
    default: true
  }
})

const visibleColumns = computed(() => 
  props.columns.filter(col => col.visible !== false)
)

const handleColumnsUpdate = (newColumns) => {
  emit('update:columns', newColumns)
}
</script>
```

---

## 📋 改造计划

### 阶段1：紧急修复（1-2小时）⚡

#### 目标
修复拖拽失效问题（主生产计划、备料计划、工序计划、预计结存）

#### 任务
1. ✅ 修改 `PageSettings.vue` 添加拖拽功能
2. ✅ 确认 `vuedraggable` 依赖已安装
3. ✅ 测试4个页面的拖拽功能

**预计影响页面**：
- `/production-planning/plan-list` 主生产计划
- `/production-planning/material-preparation` 备料计划
- `/production-planning/process-plan` 工序计划
- `/inventory/projected-balance` 预计结存

---

### 阶段2：功能补齐（2-4小时）🔧

#### 目标
为缺少列设置的页面添加功能

#### 任务列表

| 页面路径 | 页面名称 | 缺少功能 | 优先级 |
|---------|---------|---------|--------|
| `/sales/orders/customers` | 客户台账 | 列移动、列隐藏、表头筛选、排序 | 🔴 高 |
| `/sales/orders/list` | 订单列表 | 列移动 | 🔴 高 |
| `/bom/sales` | 销售BOM | 通用表格组件 | 🟡 中 |
| `/product/manual` | 产品手册 | 列设置、表头筛选排序 | 🟡 中 |
| `/material/list` | 产品物料库 | 列设置 | 🟡 中 |
| `/bom/design` | 设计BOM | 列设置、表头筛选排序 | 🟡 中 |
| `/after-sales/project-management` | 项目管理 | 列设置、表头筛选排序 | 🟢 低 |
| `/bom/production` | 生产BOM | 列设置、表头筛选排序 | 🟢 低 |
| `/manufacturing/process` | 工序 | 列设置、表头筛选排序 | 🟢 低 |

---

### 阶段3：创建通用模板（4-6小时）📦

#### 目标
创建 `StandardTablePage` 通用组件和使用模板

#### 任务
1. ✅ 创建 `StandardTablePage.vue` 组件
2. ✅ 创建使用文档和示例
3. ✅ 重构2-3个页面作为示范
4. ✅ 提供迁移指南

---

## 🛠️ 技术实现细节

### 1. vuedraggable 使用要点

```vue
<draggable
  v-model="items"           <!-- 绑定数组 -->
  item-key="prop"           <!-- 唯一key -->
  handle=".drag-handle"     <!-- 拖拽手柄 -->
  @end="handleDragEnd"      <!-- 拖拽结束事件 -->
>
  <template #item="{ element, index }">
    <div class="item">
      <el-icon class="drag-handle"><Rank /></el-icon>
      <span>{{ element.label }}</span>
    </div>
  </template>
</draggable>
```

### 2. 列顺序同步到表格

```javascript
// 方法1：直接更新columns数组
const handleColumnsUpdate = (newColumns) => {
  tableColumns.value = newColumns
}

// 方法2：保持原columns，只更新顺序
const handleColumnsUpdate = (newColumns) => {
  const columnMap = new Map(tableColumns.value.map(col => [col.prop, col]))
  tableColumns.value = newColumns.map(nc => ({
    ...columnMap.get(nc.prop),
    visible: nc.visible
  }))
}
```

### 3. 持久化列配置

```javascript
// 保存到localStorage
const saveColumnConfig = (columns) => {
  const config = {
    order: columns.map(col => col.prop),
    visibility: columns.reduce((acc, col) => {
      acc[col.prop] = col.visible
      return acc
    }, {})
  }
  localStorage.setItem('table_config_' + pageName, JSON.stringify(config))
}

// 从localStorage加载
const loadColumnConfig = () => {
  const saved = localStorage.getItem('table_config_' + pageName)
  if (saved) {
    const config = JSON.parse(saved)
    // 应用配置
  }
}
```

---

## 📊 预计结存页面错误修复

### 错误信息分析
```
❌ 加载失败: Request failed with status code 500
解析 'right' 的值时出错
解析 'left' 的值时出错
```

### 可能原因
1. **后端API错误** - 500状态码表示服务器错误
2. **CSS错误** - `right` 和 `left` 属性值解析错误

### 修复方案
1. 检查后端API `/api/projected-balance`
2. 检查页面CSS中的`fixed`属性值
3. 确保表格列配置正确

---

## ✅ 实施优先级

### 🔴 立即执行（今天）
1. 修复 `PageSettings.vue` 拖拽功能
2. 修复预计结存页面500错误

### 🟡 本周完成
1. 补齐客户台账、订单列表的列设置功能
2. 补齐产品手册、物料库的列设置功能

### 🟢 下周完成
1. 补齐其他页面的列设置功能
2. 创建 `StandardTablePage` 通用组件
3. 编写使用文档和迁移指南

---

## 📝 后续维护建议

### 1. 代码规范
- ✅ 所有新表格页面必须使用 `StandardTablePage` 或 `EnhancedTable`
- ✅ 禁止手动实现重复功能
- ✅ 统一使用 `TableColumnControl` 或 `PageSettings` 管理列

### 2. 组件版本管理
```
v1.0 - EnhancedTable (基础功能)
v2.0 - EnhancedTable + TableColumnControl (列拖拽)
v3.0 - StandardTablePage (完整方案)
```

### 3. 文档维护
- 📖 组件使用文档
- 📖 迁移指南
- 📖 常见问题FAQ

---

## 🎯 总结

### 问题根源
- ⚠️ `PageSettings` 组件的字段管理Tab缺少 `vuedraggable` 支持
- ⚠️ 不同页面使用不同的列设置组件，导致功能不统一

### 最佳方案
1. **短期**：修改 `PageSettings.vue` 添加拖拽功能（最小改动）
2. **长期**：创建 `StandardTablePage` 统一组件（架构优化）

### 预期效果
- ✅ 所有页面都支持列拖拽
- ✅ 统一的用户体验
- ✅ 易于维护和扩展
- ✅ 新页面开发更快速

---

**现在越早改，以后越轻松！** 🚀
