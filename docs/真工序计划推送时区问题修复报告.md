# 真工序计划推送到备料计划时区问题修复报告

## 问题描述

### 现象
在真工序计划推送到备料计划时，"工序计划排程日期"字段出现日期减1天的问题：
- 真工序计划的"计划排程日期"：2026-01-05
- 备料计划的"工序计划排程日期"：2026-01-04T16:00:00.000Z

### 根本原因
MySQL的DATE类型字段在存储和查询过程中存在时区转换问题：

1. **数据库存储**：MySQL存储日期时会转换为UTC时间（中国时区减8小时）
2. **后端查询**：使用`SELECT *`查询时，返回的Date对象是UTC时间
3. **推送映射**：直接使用UTC时间的Date对象进行推送
4. **前端显示**：前端接收到的仍是UTC时间格式（2026-01-04T16:00:00.000Z）

导致整个数据流中日期始终少1天。

---

## 解决方案

### 核心策略
在**数据库查询阶段**就格式化日期为中国时区YYYY-MM-DD格式，避免Date对象的UTC时区转换。

### 修复点1：真工序计划查询时格式化日期

**文件**：`/backend/services/realProcessPlanService.js`

**修改前**（第218-220行）：
```javascript
const [createdPlanRows] = await pool.execute(
  'SELECT * FROM real_process_plans WHERE id = ?',
  [result.insertId]
);
```

**修改后**：
```javascript
// ✅ 关键修复：查询时就格式化schedule_date为中国时区YYYY-MM-DD格式
const [createdPlanRows] = await pool.execute(
  `SELECT 
    id, plan_no, schedule_date, DATE_FORMAT(schedule_date, '%Y-%m-%d') as schedule_date_formatted,
    sales_order_no, customer_order_no, master_plan_no, main_plan_product_code,
    main_plan_product_name, shipping_plan_no, product_code, product_name,
    product_image, process_manager, process_name, schedule_quantity,
    product_unit, level0_demand, completion_date, promise_delivery_date,
    plan_start_date, real_plan_start_date, plan_end_date, workshop_name,
    daily_available_hours, remaining_required_hours, schedule_count,
    standard_work_hours, standard_work_quota, cumulative_schedule_qty,
    unscheduled_qty, source_page_name, source_no, previous_schedule_no,
    customer_name, level0_product_name, level0_product_code,
    level0_production_qty, product_source, bom_no, submitted_by,
    submitted_at, replenishment_qty, required_work_hours,
    daily_total_hours, daily_scheduled_hours, scheduled_work_hours,
    next_schedule_date, created_at, updated_at
  FROM real_process_plans WHERE id = ?`,
  [result.insertId]
);

// ✅ 使用格式化后的日期替换原始日期
const planData = {
  ...createdPlanRows[0],
  schedule_date: createdPlanRows[0].schedule_date_formatted // 使用YYYY-MM-DD格式
};
```

**关键改进**：
- 使用`DATE_FORMAT(schedule_date, '%Y-%m-%d')`直接在SQL查询中格式化
- 返回的`schedule_date_formatted`是字符串类型（如"2026-01-05"）
- 替换原始的Date对象，避免UTC转换

---

### 修复点2：推送时使用格式化后的数据

**文件**：`/backend/services/realProcessPlanService.js`

**修改前**（第243-246行）：
```javascript
const pushResult = await realProcessPlanToMaterialService.pushToMaterialPreparation(
  createdPlanRows[0],
  processIntervalSettings
);
```

**修改后**：
```javascript
const pushResult = await realProcessPlanToMaterialService.pushToMaterialPreparation(
  planData, // ✅ 使用格式化后的数据
  processIntervalSettings
);
```

---

### 修复点3：需求日期计算时避免UTC转换

**文件**：`/backend/services/realProcessPlanToMaterialService.js`

**修改前**（第137行）：
```javascript
demandDate = scheduleDateTime.toISOString().split('T')[0];
```

**修改后**（第137-141行）：
```javascript
// ✅ 关键修复：使用本地时间格式化，避免UTC时区转换
const year = scheduleDateTime.getFullYear();
const month = String(scheduleDateTime.getMonth() + 1).padStart(2, '0');
const day = String(scheduleDateTime.getDate()).padStart(2, '0');
demandDate = `${year}-${month}-${day}`;
```

**说明**：
- `toISOString()`会将本地时间转换为UTC时间，导致日期错误
- 使用`getFullYear()`、`getMonth()`、`getDate()`获取本地时间组件
- 手动拼接为YYYY-MM-DD格式

---

### 修复点4：工序计划排程日期格式化

**文件**：`/backend/services/realProcessPlanToMaterialService.js`

**修改**（第163-180行）：
```javascript
// ✅ 格式化工序计划排程日期为YYYY-MM-DD（避免时区问题）
let formattedScheduleDate = null;
if (scheduleDate) {
  // ✅ 关键：scheduleDate可能是Date对象或字符串，统一处理
  let dateObj;
  if (scheduleDate instanceof Date) {
    dateObj = scheduleDate;
  } else {
    // 字符串格式，可能是 YYYY-MM-DD 或 ISO格式
    dateObj = new Date(scheduleDate);
  }
  
  // ✅ 使用本地时间获取年月日，避免UTC时区转换导致日期减1
  const year = dateObj.getFullYear();
  const month = String(dateObj.getMonth() + 1).padStart(2, '0');
  const day = String(dateObj.getDate()).padStart(2, '0');
  formattedScheduleDate = `${year}-${month}-${day}`;
}
```

**说明**：
- 由于前面已在查询时格式化，`scheduleDate`现在是字符串
- 但为了兼容性，仍保留Date对象的处理逻辑
- 统一使用本地时间格式化为YYYY-MM-DD

---

### 修复点5：备料计划查询时格式化日期字段

**文件**：
- `/backend/services/materialPreparationPlanService.js`
- `/backend/routes/materialPreparationPlans.js`

**问题**：
原始查询使用`SELECT *`，返回的`process_schedule_date`、`demand_date`、`promise_delivery_date`是UTC时间的Date对象，导致前端显示为ISO格式（如`2026-01-04T16:00:00.000Z`）。

**修改前**（`materialPreparationPlanService.js`第54行）：
```javascript
const dataSQL = `
  SELECT * FROM material_preparation_plans 
  ${whereSQL}
  ORDER BY created_at DESC 
  LIMIT ${limit} OFFSET ${offset}
`;
```

**修改后**：
```javascript
const dataSQL = `
  SELECT 
    id, plan_no, source_plan_no, source_process_plan_no,
    parent_code, parent_name, parent_schedule_quantity,
    material_code, material_name, material_source, material_unit,
    demand_quantity, need_mrp, realtime_stock, projected_balance,
    available_stock, replenishment_quantity, source_process,
    workshop_name, parent_process_name, process_interval_hours,
    process_interval_unit,
    DATE_FORMAT(process_schedule_date, '%Y-%m-%d') as process_schedule_date,
    DATE_FORMAT(demand_date, '%Y-%m-%d') as demand_date,
    push_to_purchase, push_to_process, sales_order_no, customer_order_no,
    main_plan_product_code, main_plan_product_name, main_plan_quantity,
    DATE_FORMAT(promise_delivery_date, '%Y-%m-%d') as promise_delivery_date,
    remark, created_by, created_at, updated_by, updated_at
  FROM material_preparation_plans 
  ${whereSQL}
  ORDER BY created_at DESC 
  LIMIT ${limit} OFFSET ${offset}
`;
```

**关键改进**：
- 使用`DATE_FORMAT()`格式化所有日期字段
- 返回的是字符串类型（如“2026-01-05”）
- 前端直接显示，不需要额外处理

**同步修改**：
- `getById()` 方法（第119行）
- `/routes/materialPreparationPlans.js` 的推送查询（第162行）

---

## 测试验证

### 测试脚本
创建了`/backend/test_date_format.js`用于验证修复效果。

### 测试结果
```
📊 真工序计划数据:
   计划排程日期 (schedule_date): Wed Jan 07 2026 00:00:00 GMT+0800 (中国标准时间)
   格式化后: 2026-01-07

📊 从真工序计划推送的备料计划数量: 3

备料计划详情:
  1. MPP202512131765610000609633
     物料: 470001A - 6001背头套袋件
     工序计划排程日期 (process_schedule_date): Wed Jan 07 2026 00:00:00 GMT+0800 (中国标准时间)
     格式化后: 2026-01-07
     ✅ 日期匹配！

  2. MPP202512131765610000610072
     物料: 470002A - 6001主架套袋件
     工序计划排程日期 (process_schedule_date): Wed Jan 07 2026 00:00:00 GMT+0800 (中国标准时间)
     格式化后: 2026-01-07
     ✅ 日期匹配！

  3. MPP202512131765610000611087
     物料: 511442B - 外箱
     工序计划排程日期 (process_schedule_date): Wed Jan 07 2026 00:00:00 GMT+0800 (中国标准时间)
     格式化后: 2026-01-07
     ✅ 日期匹配！
```

### 验证结论
✅ 真工序计划的"计划排程日期"与备料计划的"工序计划排程日期"完全匹配，日期格式为YYYY-MM-DD，无时区转换问题。

---

## 技术规范总结

### MySQL日期字段时区处理强制规范

**核心原则**：数据库存储、后端查询、前端显示全程使用中国本地时区（YYYY-MM-DD），禁止使用UTC时间或ISO字符串。

#### 1. 后端查询时必须格式化日期
```sql
-- ✅ 正确
SELECT DATE_FORMAT(date, '%Y-%m-%d') as date FROM table_name

-- ❌ 错误
SELECT date FROM table_name
```

#### 2. 后端比较日期时使用字符串格式
```javascript
// ✅ 正确
WHERE date <= '2026-01-06'

// ❌ 错误
WHERE date <= new Date('2026-01-06')
```

#### 3. 前端传递日期参数时使用YYYY-MM-DD格式
```javascript
// ✅ 正确
const dateStr = '2026-01-06'

// ❌ 错误
const dateStr = new Date().toISOString()
```

#### 4. 日期转换统一使用本地时区
```javascript
// ✅ 正确
const dateStr = `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}`

// ❌ 错误
const dateStr = date.toISOString().split('T')[0]
```

### 适用场景
- 所有日期字段的数据库查询
- 所有日期比较逻辑
- 前后端日期参数传递
- 计划开始/结束日期计算
- 工序能力负荷表日期查询

### 违反后果
- 日期显示错误（差1天）
- 计算逻辑错误
- 数据匹配失败
- 排程结果不准确

---

## 相关文件清单

### 修改的文件
1. `/backend/services/realProcessPlanService.js` - 真工序计划服务（查询时格式化schedule_date）
2. `/backend/services/realProcessPlanToMaterialService.js` - 真工序计划推送到备料计划服务（demandDate格式化）
3. `/backend/services/materialPreparationPlanService.js` - 备料计划服务（查询时格式化所有日期字段）
4. `/backend/routes/materialPreparationPlans.js` - 备料计划路由（推送查询时格式化日期）

### 测试文件
1. `/backend/test_date_format.js` - 日期格式测试脚本

---

## 数据流闭环状态

### 完整数据流
```
销售订单 
  ↓ (执行排程)
主生产计划 
  ↓ (自动创建，物料来源=自制)
备料计划 
  ↓ (自动创建，计算排程数量>0)
真工序计划 
  ↓ (自动推送3个子件，日期格式YYYY-MM-DD)
备料计划（子件：470001A、470002A、511442B）
```

### 关键修复点
1. ✅ 主生产计划 → 备料计划：自动推断物料来源
2. ✅ 备料计划 → 真工序计划：物料来源="自制"时自动推送
3. ✅ 真工序计划 → 备料计划：通过BOM_NO查询子件，日期格式化为YYYY-MM-DD
4. ✅ 6个新增字段完整传递：客户订单编号、主计划产品编号、主计划产品名称、订单承诺交期、销售订单编号、客户名称

---

## 后续建议

### 1. 全局日期字段审查
建议对项目中所有日期字段进行审查，统一使用`DATE_FORMAT()`函数查询，确保时区一致性。

### 2. 日期工具函数封装
建议封装统一的日期格式化工具函数：
```javascript
// utils/dateFormatter.js
function formatDateToYYYYMMDD(date) {
  if (!date) return null;
  const dateObj = date instanceof Date ? date : new Date(date);
  const year = dateObj.getFullYear();
  const month = String(dateObj.getMonth() + 1).padStart(2, '0');
  const day = String(dateObj.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}
```

### 3. 前端日期显示统一
前端日期选择器和显示组件应统一使用YYYY-MM-DD格式，避免显示ISO格式。

---

## 修复时间
2025-12-13

## 修复人员
AI Assistant (Qoder)

## 状态
✅ 已完成并测试通过
