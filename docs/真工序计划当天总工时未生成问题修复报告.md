# 真工序计划当天总工时未生成问题修复报告

**修复日期**: 2025-12-14  
**报告人**: AI Assistant  
**修复状态**: ✅ 已完成并推送到远程仓库  

---

## 一、问题描述

### 1.1 问题表现

用户报告: 真工序计划编号`RPP2025951158402`的计划排程日期为`2026-01-03`,但**当天总工时(daily_total_hours)字段为0**,未按预期生成。

### 1.2 预期行为

应该从工序能力负荷表查询:
- 工序名称: `组装`
- 日期: `2026-01-03`
- 班次工时(work_shift): `8`
- 可用工位数(available_workstations): `2`
- **当天总工时(total_hours)**: `8 * 2 = 16`

### 1.3 实际数据

**真工序计划表** (`real_process_plans`):
```sql
SELECT plan_no, process_name, schedule_date, daily_total_hours
FROM real_process_plans 
WHERE plan_no = 'RPP2025951158402';
```
结果:
```
plan_no: RPP2025951158402
process_name: 组装
schedule_date: 2026-01-03
daily_total_hours: 0.00   ❌ 错误,应该是16.00
```

**工序能力负荷表** (`process_capacity_load`):
```sql
SELECT process_name, date, work_shift, available_workstations, total_hours
FROM process_capacity_load 
WHERE process_name = '组装' AND date = '2026-01-03';
```
结果:
```
process_name: 组装
date: 2026-01-03
work_shift: 8.00
available_workstations: 2
total_hours: 0.00   ⚠️ 注意:这个字段也是0,但剩余工时=16是正确的
```

---

## 二、根本原因分析

### 2.1 数据流路径

真工序计划的创建路径:
```
备料计划 (material_preparation_plans)
    ↓ 推送
真工序计划 (real_process_plans)
```

### 2.2 问题定位

**文件**: `backend/services/materialPreparationPlanService.js`  
**方法**: `pushToRealProcessPlan()` (第868-1076行)

在第1015-1043行创建`realProcessPlanData`时:
```javascript
// ❌ 问题代码 (修复前)
const realProcessPlanData = {
  planNo: realProcessPlanNo,
  salesOrderNo: data.salesOrderNo,
  // ... 其他字段
  scheduleDate: scheduleDate,
  customerName: data.customerName,
  sourceNo: data.planNo,
  scheduleCount: 1,
  // ❌ 缺少dailyTotalHours字段,默认为0
  submittedBy: data.createdBy || 'admin',
  submittedAt: new Date().toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai', hour12: false })
};
```

**核心问题**:
1. 没有查询工序能力负荷表获取当天总工时
2. `dailyTotalHours`字段未传入,使用默认值0
3. 导致真工序计划创建时该字段为0

---

## 三、修复方案

### 3.1 修复逻辑

在创建`realProcessPlanData`之前,新增查询工序能力负荷表的逻辑:

```javascript
// ✅ 修复代码 (新增)
// 查询当天总工时 (从工序能力负荷表)
let dailyTotalHours = 0;
if (scheduleDate && data.sourceProcess) {
  try {
    const [capacityRows] = await connection.execute(`
      SELECT work_shift, available_workstations 
      FROM process_capacity_load 
      WHERE process_name = ? AND DATE_FORMAT(date, '%Y-%m-%d') = ?
      LIMIT 1
    `, [data.sourceProcess, scheduleDate]);
    
    if (capacityRows.length > 0) {
      const workShift = parseFloat(capacityRows[0].work_shift || 0);
      const availableWorkstations = parseFloat(capacityRows[0].available_workstations || 0);
      dailyTotalHours = parseFloat((workShift * availableWorkstations).toFixed(2));
      console.log(`✅ 查询当天总工时: 工序=${data.sourceProcess}, 日期=${scheduleDate}, 班次=${workShift}, 工位数=${availableWorkstations}, 总工时=${dailyTotalHours}`);
    } else {
      console.warn(`⚠️ 未查询到工序能力负荷记录: 工序=${data.sourceProcess}, 日期=${scheduleDate}`);
    }
  } catch (error) {
    console.error(`❌ 查询当天总工时失败:`, error.message);
  }
}

// 创建真工序计划数据
const realProcessPlanData = {
  planNo: realProcessPlanNo,
  // ... 其他字段
  scheduleDate: scheduleDate,
  customerName: data.customerName,
  sourceNo: data.planNo,
  scheduleCount: 1,
  dailyTotalHours: dailyTotalHours,  // ✅ 新增:当天总工时
  submittedBy: data.createdBy || 'admin',
  submittedAt: new Date().toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai', hour12: false })
};
```

### 3.2 核心改动点

1. **新增查询逻辑** (第1015-1038行):
   - 根据`scheduleDate`和`sourceProcess`查询工序能力负荷表
   - 获取`work_shift`和`available_workstations`
   - 计算`dailyTotalHours = work_shift * available_workstations`
   - 添加日志输出,方便排查

2. **传入数据** (第1066行):
   - 将计算出的`dailyTotalHours`传入`realProcessPlanData`

---

## 四、测试验证

### 4.1 测试脚本

创建测试文件: `test_daily_total_hours.js`

**测试流程**:
1. 查询工序能力负荷表,获取预期总工时
2. 创建测试备料计划
3. 触发推送到真工序计划
4. 验证真工序计划的`daily_total_hours`字段
5. 清理测试数据

### 4.2 测试结果

```
🧪 开始测试真工序计划当天总工时生成功能

📊 第一步:查询工序能力负荷表数据
   工序: 组装
   日期: 2026-01-03
   班次工时: 8.00
   可用工位数: 2
   当前total_hours: 0.00
   预期总工时: 16

📝 第二步:创建测试备料计划
   测试备料计划已创建: TEST_MPP_1765687342841

🚀 第三步:触发推送到真工序计划
   备料计划数据: {
     planNo: 'TEST_MPP_1765687342841',
     materialCode: 'M001',
     materialName: '测试物料',
     sourceProcess: '组装',
     demandQuantity: '100.0000',
     availableStock: '30.0000',
     replenishmentQty: 70
   }

✅ 查询到工时数据: 定时工额=10, 定额工时=0.5
🧮 计算需求工时: 70 / 10 = 7
✅ 计划结束日期: 2026-01-03
✅ 计划开始日期: 2026-01-02, 累计工时: 16.00
✅ 查询当天总工时: 工序=组装, 日期=2026-01-03, 班次=8, 工位数=2, 总工时=16   ⬅️ 关键日志

真工序计划创建成功, ID: 964, 编号: RPP2025342865150

🔍 第四步:验证真工序计划的当天总工时
   真工序计划数据:
     计划编号: RPP2025342865150
     工序名称: 组装
     计划排程日期: 2026-01-03
     当天总工时 (daily_total_hours): 16.00   ✅ 正确！
     计划排程工时 (scheduled_work_hours): 0.00
     计划排程数量 (schedule_quantity): 0.00

   ✅ 测试通过！当天总工时正确生成: 16 (预期: 16)   ⬅️ 测试通过

🧹 第五步:清理测试数据
   已删除测试数据

🎉 测试完成！
```

**测试结论**: ✅ **修复成功！当天总工时正确生成为16.00**

---

## 五、修改文件清单

### 5.1 后端修改

**文件**: `backend/services/materialPreparationPlanService.js`

**修改位置**: 第1012-1066行

**修改内容**:
- 新增第1015-1038行: 查询当天总工时逻辑
- 修改第1066行: 传入`dailyTotalHours`字段

**代码差异**:
```diff
       // 计划排程日期 = 真计划开始日期
       const scheduleDate = realPlanStartDate;
 
+      // ✅ 查询当天总工时 (从工序能力负荷表)
+      let dailyTotalHours = 0;
+      if (scheduleDate && data.sourceProcess) {
+        try {
+          const [capacityRows] = await connection.execute(`
+            SELECT work_shift, available_workstations 
+            FROM process_capacity_load 
+            WHERE process_name = ? AND DATE_FORMAT(date, '%Y-%m-%d') = ?
+            LIMIT 1
+          `, [data.sourceProcess, scheduleDate]);
+          
+          if (capacityRows.length > 0) {
+            const workShift = parseFloat(capacityRows[0].work_shift || 0);
+            const availableWorkstations = parseFloat(capacityRows[0].available_workstations || 0);
+            dailyTotalHours = parseFloat((workShift * availableWorkstations).toFixed(2));
+            console.log(`✅ 查询当天总工时: 工序=${data.sourceProcess}, 日期=${scheduleDate}, 班次=${workShift}, 工位数=${availableWorkstations}, 总工时=${dailyTotalHours}`);
+          } else {
+            console.warn(`⚠️ 未查询到工序能力负荷记录: 工序=${data.sourceProcess}, 日期=${scheduleDate}`);
+          }
+        } catch (error) {
+          console.error(`❌ 查询当天总工时失败:`, error.message);
+        }
+      }
+
       // 创建真工序计划数据
       const realProcessPlanData = {
         planNo: realProcessPlanNo,
         ...
         customerName: data.customerName,
         sourceNo: data.planNo,
         scheduleCount: 1,
+        dailyTotalHours: dailyTotalHours,  // ✅ 新增:当天总工时
         submittedBy: data.createdBy || 'admin',
         submittedAt: new Date().toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai', hour12: false })
       };
```

### 5.2 测试文件

**新增文件**: `test_daily_total_hours.js`

**用途**: 自动化测试真工序计划当天总工时生成功能

**文件内容**: 188行完整测试脚本

---

## 六、影响范围

### 6.1 直接影响

**所有通过备料计划推送生成的真工序计划**,现在都会:
- ✅ 正确查询工序能力负荷表
- ✅ 计算并填充`daily_total_hours`字段
- ✅ 值为: `work_shift * available_workstations`

### 6.2 数据影响

- **已有数据**: 不影响,已创建的真工序计划`daily_total_hours`仍为0
- **新创建数据**: 所有新推送的真工序计划都会正确生成该字段

### 6.3 特殊情况处理

1. **工序能力负荷表无记录**:
   - 输出警告日志: `⚠️ 未查询到工序能力负荷记录`
   - `dailyTotalHours`保持为0

2. **查询失败**:
   - 捕获异常,输出错误日志
   - `dailyTotalHours`保持为0,不阻塞推送流程

---

## 七、后续建议

### 7.1 数据修复 (可选)

如需修复已有的`daily_total_hours=0`的记录,可执行:

```sql
UPDATE real_process_plans rpp
INNER JOIN process_capacity_load pcl 
  ON rpp.process_name = pcl.process_name 
  AND DATE(rpp.schedule_date) = DATE(pcl.date)
SET rpp.daily_total_hours = pcl.work_shift * pcl.available_workstations
WHERE rpp.daily_total_hours = 0 
  AND rpp.schedule_date IS NOT NULL;
```

### 7.2 工序能力负荷表total_hours字段

注意到`process_capacity_load`表的`total_hours`字段也全是0,建议:

1. **检查该字段是否应该自动计算**:
   ```sql
   UPDATE process_capacity_load
   SET total_hours = work_shift * available_workstations
   WHERE total_hours = 0;
   ```

2. **或在插入/更新时触发计算**:
   - 添加数据库触发器
   - 或在Service层自动计算

---

## 八、Git提交记录

**提交信息**:
```
🐛 修复真工序计划当天总工时未生成问题

## 问题描述
真工序计划编号RPP2025951158402的计划排程日期2026-01-03,当天总工时(daily_total_hours)为0,未按预期生成。
实际应该查询工序能力负荷表,计算总工时 = 班次工时 * 可用工位数 = 8 * 2 = 16

## 根本原因
备料计划推送到真工序计划时(materialPreparationPlanService.js),没有查询工序能力负荷表获取当天总工时,
导致dailyTotalHours字段使用默认值0

## 修复方案
在备料计划推送到真工序计划方法中:
1. 根据scheduleDate和sourceProcess查询工序能力负荷表
2. 获取work_shift和available_workstations
3. 计算dailyTotalHours = work_shift * available_workstations
4. 传入realProcessPlanData

## 修改文件
- backend/services/materialPreparationPlanService.js (第1012-1064行)
  - 新增查询当天总工时逻辑
  - 将dailyTotalHours传入真工序计划数据

## 测试验证
创建test_daily_total_hours.js测试脚本:
✅ 测试通过！当天总工时正确生成: 16 (预期: 16)

## 影响范围
- 所有通过备料计划推送生成的真工序计划,现在都会正确生成daily_total_hours字段
- 不影响已有数据,仅影响新创建的记录
```

**提交ID**: `2ee9f5c`  
**分支**: `main`  
**推送状态**: ✅ 已推送到远程仓库

---

## 九、总结

### 9.1 修复成果

✅ **成功修复**真工序计划当天总工时未生成问题  
✅ **通过自动化测试验证**修复效果  
✅ **代码已推送到远程仓库**,作为恢复节点  

### 9.2 关键技术点

1. **数据流追踪**: 从备料计划 → 真工序计划的推送逻辑
2. **跨表查询**: 根据工序名称和日期查询工序能力负荷表
3. **字段计算**: `dailyTotalHours = work_shift * available_workstations`
4. **异常处理**: 查询失败不阻塞推送流程

### 9.3 经验总结

1. **字段初始化要完整**: 创建记录时,应填充所有必要字段,避免使用默认值
2. **跨表数据查询**: 需要的数据应主动查询,而不是假设会从其他地方传入
3. **日志输出重要**: 详细的日志帮助快速定位问题
4. **自动化测试必要**: 通过测试脚本验证修复效果,避免回归

---

**报告完成时间**: 2025-12-14 12:45  
**下次检查点**: 用户测试新数据生成情况  
**状态**: ✅ 完成
